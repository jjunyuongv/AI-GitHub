# 07 · 답변 품질

2026-08-21 ~ · [plan.md](../../plan.md) 로 돌아가기

포함된 절:
- 0단계: 답변 품질 판정 기준 고정 - 2026-08-21
- 0단계 기준선 측정 — 자가 새는 것을 확인했다 - 2026-08-21
- 거절 채점기 교체 — 무과금 재채점으로 기준선을 다시 썼다 - 2026-08-22
- 1단계 착수 전 조사 — 소스가 없다는 것을 확인했다 - 2026-08-22
- 소스 보관 — 스냅샷 단위로 원문을 남긴다 - 2026-08-22
- 죽은 import 정리 — `rule_version` 을 지웠다 - 2026-08-22
- 1단계: 순수 tool use — 구현과 무과금 검증 (측정 전 정지) - 2026-08-22
- 1단계 측정 — 실비 $2.1259, 그리고 거절 자가 또 샜다 - 2026-08-22

<!-- BODY -->

## 0단계: 답변 품질 판정 기준 고정 - 2026-08-21

tool use 를 붙이기 전에 **자를 먼저 만든다.** tool use 는 검색을 반복하는 경로를 만드는데,
그 경로의 고유한 실패는 *없는 것을 계속 뒤지다 지어내는 것*이다.

**지금 있는 자는 그걸 못 잡는다.** `test_citation_quality.py` 의 인용 정확도는 "답변이
정답 파일을 짚었는가"를 세는데, 정답이 애초에 없는 질의에서는 짚을 파일이 없다 —
날조와 침묵이 똑같이 0점으로 보인다. 그래서 **거절 축**을 따로 만든다.

이 단계에서 tool use 는 붙이지 않는다. 브랜치 `eval/answer-quality`.

### 계획

- [ ] 거절 질의 18개 (저장소당 6개). 기존 목록에 **섞지 않는다** — 섞으면 Recall@8 분모가
      바뀌어 `search_evals.jsonl` 의 기존 기록과 비교가 끊긴다.
      `EVAL_SET_VERSION` 은 2 유지, `ABSENT_SET_VERSION = 1` 신설
      → verify: 로컬 소스 캐시 전수 검색으로 부재를 확인하고 `why_absent` 에 근거를 남긴다
- [ ] `claude_client.REFUSAL_PHRASES` — 프롬프트가 강제하는 거절 문구를 상수로.
      **f-string 으로 프롬프트에 끼워 넣지 않는다**(정합성 테스트가 동어반복이 된다)
      → verify: 프롬프트 포함 검사 1건 + 변이로 실제로 깨지는지 확인
- [ ] 하네스 다중 세트·가변 팔. 팔은 **저장소 이름이 아니라 번들 토큰 실측**으로 유도
      (CLAUDE.md §7) → verify: apns4j 만 2팔, 나머지는 1팔로 돈다
- [ ] `llm_ms`·`refusal_accuracy`·질문당 비용·질문당 지연을 결과에 담는다
- [ ] 판정 규칙 A/B/C 를 docstring 에 **측정 전에** 고정
- [ ] 무과금 자가 검증 (`test_citation_scoring.py`)
      → verify: `-m` 없이 도는 테스트로 채점 함수의 양방향을 다 잡는다
- [ ] 기준선 측정은 **승인 후**. 예상 비용을 먼저 보고한다

### 결과 — 자를 만들고 무과금으로 검증했다 (기준선 측정은 미실행)

- [x] 거절 질의 18개. `ABSENT_SET_VERSION = 1` · `ABSENT_SETS` 신설.
      `EVAL_SET_VERSION` 은 2 그대로 — **인용 정확도의 모수에서 absent 행을 뺐으므로**
      지난 두 기록(0.75 / 0.8125)과 같은 칸에서 계속 비교된다
- [x] 질의는 **로컬 소스 캐시 전수 검색**으로 골랐다. 관련 낱말이 0회인 것만 채택하고
      `why_absent` 에 근거를 남겼다
- [x] `claude_client.REFUSAL_PHRASES` — 프롬프트는 리터럴 유지, 포함 검사 1건 추가
- [x] 하네스 다중 세트·가변 팔. 팔은 `measure_bundle()` 로 잰 토큰으로 유도한다
- [x] `llm_ms`·`refusal_accuracy`·`avg_cost_usd`·`avg_llm_ms` 를 결과에 담았다
- [x] 판정 기준 A/B/C 를 docstring 에 고정 (측정 **전에**)
- [x] `tests/test_citation_scoring.py` 15건 (무과금, `-m` 없이 돈다)
- [x] 검증: **337 passed / 10 skipped** · ruff(E9,F) 통과. LLM 실호출 없음
- [x] 기준선 측정 — 실비 $0.8885. 결과는 아래 절

### 정한 것과 그 이유

**팔을 저장소 이름으로 가르지 않는다.** `measure_bundle()` 로 잰 번들 토큰이 임계값
이하일 때만 `full` 팔을 붙인다 — 프로덕션이 우회를 판정할 때 쓰는 바로 그 함수라
하네스와 서비스의 기준이 갈리지 않는다. 임계값을 바꾸면 팔도 자동으로 따라간다.
실측: apns4j 34,467 · air 62,380 · marryday 1,057,764 (임계값 57,000).

**거절 문구를 프롬프트에 f-string 으로 끼워 넣지 않았다.** 끼워 넣으면 정합성이
자동으로 맞아 보이지만 그 순간 검사가 동어반복이 되어, 문구가 갈라져 하네스가 조용히
0점을 매기는 상황을 못 잡는다. 리터럴로 두고 포함 여부를 검사하는 쪽이 **실제로 깨진다**
(아래 변이에서 확인).

**거절 질의를 기존 목록에 섞지 않은 진짜 이유는 두 개다.** 하나는 Recall@8 분모가
바뀌는 것이고, 다른 하나는 `_cited()` 가 `answers` 없는 질의에서 **언제나 False** 라는
것이다. 섞으면 그 False 들이 분자에서 빠진 채 분모만 늘려 인용 정확도가 조용히
희석되고, 그 하락이 개선 실패로 오독된다. 이걸 테스트로 못 박았다.

### 변이 확인 — 넷 다 실제로 깨졌다

전례가 있어(`06-stage5.md:210`) **변이가 정말 파일을 바꿨는지부터** sha256 으로 확인했다.
원본은 워킹트리 사본을 떠 두고 복원·대조했다(`git show` 를 쓰지 않는다 — `core.autocrlf`
때문에 blob 과 워킹트리의 바이트가 다르다). 넷 다 복원 후 바이트 일치.

| 변이 | 깨진 테스트 |
|---|---|
| `NO_BASIS_PHRASE` 끝에 마침표 한 글자 | `test_refusal_phrases_are_in_the_chat_prompt` |
| `_summarize` 에서 absent 행 제외 제거 | `test_absent_rows_stay_out_of_citation_accuracy` |
| `_score` 가 kind 를 안 보게 | `test_score_picks_the_ruler_by_kind` |
| `_cited` 의 빈 `answers` 기본값을 빈 접미사로 | `test_cited_is_always_false_without_answers` |

### 과금 전에 무과금으로 미리 태운 것

보고서 출력·팔 선택·집계를 가짜 행으로 한 번 돌렸다. 포맷 문자열 오류 하나 때문에
$0.88 짜리 측정이 마지막 줄에서 죽는 것을 막으려는 것이다. 1팔·2팔 두 경로 다 확인.

`test_line_accuracy.py` 는 `("rag", "full")` 을 하드코딩하고 있어 `full` 없는 기록에서
`KeyError` 로 죽었다. `rec.get("arms", ("rag", "full"))` 로 고치고, 가짜 기록 2건
(새 모양 1팔 · 옛 모양 팔 키 없음)으로 확인했다.

### 기준선 예상 비용 (미실행)

지난 기록 기준 rag $0.00746/질의 · full $0.01716/질의.

| 세트 | 질의 | 팔 | 호출 | 예상 |
|---|---|---|---|---|
| apns4j | 22 | rag, full | 44 | $0.54 |
| air | 23 | rag | 23 | $0.17 |
| marryday | 22 | rag | 22 | $0.16 |
| **합계** | | | **89** | **≈ $0.88** |

air·marryday 는 RAG 색인이 없으면 `_ensure_index` 가 먼저 만든다(무과금이지만 CPU 로
오래 걸린다). sonnet-5 도입가는 2026-08-31 까지다 — 그 뒤에 재면 1.5배가 된다.

## 0단계 기준선 측정 — 자가 새는 것을 확인했다 - 2026-08-21

**이 측정은 1단계 tool use 의 비교 기준선이다.** 이 숫자가 없으면 tool use 를 붙여도
좋아졌는지 나빠졌는지 말할 수 없다. 이번 실행에서 무언가를 채택하거나 기각하지 않는다.

### 판정 규칙 — 측정 **전에** 고정했다

`tests/test_citation_quality.py` docstring 에 박아 두고 측정을 돌렸다. 결과를 보고
정하면 측정이 아니라 사후 합리화가 된다.

    A. 인용 정확도가 기준선보다 떨어지면 기각
    B. A 가 동률이면 거절 정확도를 본다. 떨어지면 기각
    C. A·거절이 올라도 질문당 비용이 COST_RATIO_LIMIT(3.0)배를 넘으면
       전면 도입하지 않고 "큰 저장소 전용"으로만 켠다

### 사전 준비

색인 생성과 과금 측정을 한 실행에 섞지 않았다 — marryday 는 4,365청크라 오래 걸리고,
그 도중에 죽으면 이미 쓴 API 비용이 날아간다. 세 세트 다 **현행 규칙(822bb217)의
활성 빌드가 이미 있어** 재색인은 필요 없었다 (air 236 · marryday 4,365 · apns4j 168청크).
`active_build_id()` 는 규칙 버전을 보지 않으므로 낡은 색인도 '활성'이다 — 그래서
빌드의 `chunk_rule` 을 따로 대조했다.

### 결과 (sonnet-5, 8분 32초, 89회 호출)

| 세트 | 팔 | 인용 | 한국어 | 식별자 | 거절 | 질문당 비용 | 질문당 지연 |
|---|---|---|---|---|---|---|---|
| air | rag | 0.7059 | 0.5833 | 1.0 | 0.6667 | $0.008155 | 5,258ms |
| marryday | rag | 0.6875 | 0.700 | 0.6667 | 0.6667 | $0.009586 | 5,896ms |
| apns4j | rag | 0.7500 | 0.600 | 1.0 | **0.0** | $0.007155 | 5,078ms |
| apns4j | full | 0.9375 | 0.900 | 1.0 | 0.1667 | $0.015119 | 5,363ms |

실비 **$0.8885** (예상 $0.88 대비 +1%). air $0.1876 · marryday $0.2109 ·
apns4j rag $0.1574 + full $0.3326. rag 질문당은 예상 $0.00746 대비 $0.0072~0.0096
(marryday 가 스니펫이 커서 높다), full 은 예상 $0.01716 대비 $0.01512 로 12% 낮았다.

### 거절 실패 15건을 전부 읽었다 — **날조 0건, 전부 자의 문제다**

이것을 가르지 않으면 기준선을 잘못 읽는다. 실패한 15건 모두 모델이 "없다"고 정확히
답했고, **규정 문구를 글자 그대로 쓰지 않았을 뿐이다.** 짧게 답하려다 생략한 것도
아니다 — 오히려 근거를 더 길게 댔다.

    [ap_ab_05] 검색된 코드 범위에는 프록시 서버 설정 관련 내용이 없습니다.
               SecuritySocketFactory 는 ... sslSocketFactory.createSocket(host, port)
               로 직접 대상 호스트에 연결하는 방식입니다(119-123행).

패턴이 하나다. 규정 문구는 `검색된 범위에는 없습니다` 인데 모델은 거의 항상
**"검색된 `코드` 범위에는 … 없습니다"** 로 쓴다 — 한 단어가 끼어들어 연속 문자열
매칭이 깨진다. apns4j RAG 가 0.0 인 것은 6건 전부 이 변형이었기 때문이다.

**`full` 팔은 구조적으로 더 심하다.** 전체 소스가 왔을 때 "검색된 범위" 표현을 쓰지
말라고 프롬프트가 막고 있어서, 모델은 "저장소 전체 소스를 확인한 결과 … 존재하지
않습니다"로 답한다. 이건 규정된 두 문구 어디에도 안 걸린다 —
**프롬프트가 요구하는 대로 답할수록 자가 0점을 주는 구조다.**

### 자의 한계 — 절대값으로 읽지 말 것

문구 매칭은 "지어내지 않았는가"의 **대리 지표**다. 이번 측정이 그 한계를 실측으로
보여줬다: 거절 정확도 0.0 은 "6건 다 지어냈다"가 아니라 "6건 다 규정 표현을 안 썼다"였다.
두 팔에 같은 자를 대므로 **비교**에는 쓸 수 있지만, 절대값을 품질로 읽으면 정반대로
틀린다. 특히 apns4j RAG 의 0.0 은 바닥 효과라 1단계에서 올라갈 수밖에 없다 —
그 상승을 tool use 의 성과로 읽으면 안 된다.

**자를 고치는 것은 다음 과제로 남긴다.** 지금 고치면 기준선과 1단계가 다른 자로
재게 된다. 고친다면 (a) 프롬프트 문구를 모델이 실제로 쓰는 표현에 맞추거나,
(b) 매칭을 정규식으로 느슨하게 하거나, (c) 전체 주입용 거절 문구를 따로 규정하는
세 갈래다. 어느 쪽이든 **자를 바꾼 뒤에는 기준선을 다시 재야 한다.**

### 부재 근거 하나가 불완전했다 — `ap_ab_02`

`http2`·`HTTP/2` 0회만 확인했는데, 프로토콜 이름이 아니라 **엔드포인트 주소**로 있었다:

    RESTFUL_PRODUCTION("api.push.apple.com", 443),   // HTTP/2 게이트웨이

전송 구현은 여전히 레거시 바이너리뿐이라 "없는 것"이라는 판정 자체는 맞지만
`why_absent` 는 틀렸다. **모델이 나보다 정확했다** — 게이트웨이 상수는 있으나 실제
요청을 만드는 로직은 없다고 갈라 답했다. 부재를 확인할 때 기능 이름만 찾으면
이렇게 샌다. 도메인 식별자(호스트·포트·상수명)도 함께 봐야 한다.

### 함정: `-m billed` 만 쓰면 조용히 아무것도 안 돈다

docstring 이 적어 둔 명령이 그랬다(0단계 이전부터). `conftest` 의 opt-in 게이트는
마커마다 **따로** 물어서, 이 모듈에 걸린 `evaluation` 마커가 `-m` 에 없으면 그것만으로
건너뛴다. 0.9초에 `sss` 로 끝나고 로그 파일도 안 생긴다. 과금이 없었던 것은 다행이지만,
"측정을 돌렸다"고 착각하기 딱 좋은 모양이다.
`-m "billed and evaluation"` 으로 고치고 docstring 에 이 사실을 적었다.

## 거절 채점기 교체 — 무과금 재채점으로 기준선을 다시 썼다 - 2026-08-22

**채점기만 고쳤다. `CHAT_SYSTEM_PROMPT` 는 한 글자도 건드리지 않았다** — 프롬프트를
고치면 재는 대상이 바뀌어 인용 정확도까지 흔들리고 89회를 다시 돌려야 한다.

### 재채점 경로 — API 호출 0회, $0

`Back/logs/citation_evals.jsonl` 에 답변 원문 89건이 그대로 있다.
`tests/test_rescore_refusals.py` 가 그것을 다시 매긴다 (`-m evaluation`, 무과금).
`test_line_accuracy.py` 가 같은 자산을 같은 방식으로 재사용하는 것과 같은 구조다.
**채점기를 고칠 때마다 LLM 을 다시 부를 이유가 없다.**

### 채점기는 문장 구조에서 유도했다 — 정규식을 늘린 것이 아니다

거절 문장은 예외 없이 **"[어디를 봤는가] + [거기에 없다]"** 두 조각이다. 그래서 조각을
따로 정의하고 한 문장 안에서 이 순서로 나타나는지만 본다.

| 조각 | 상수 | 무엇 |
|---|---|---|
| 스코프 | `REFUSAL_SCOPES` (5) | 검색된… · 검색 결과 · 주어진/제공된 정보·코드·소스 · 전체 소스 · 코드 전체 |
| 부정 | `REFUSAL_NEGATIONS` (8) | 없다 · 보이지 않 · 존재하지 않 · 찾을 수 없 · 확인되지 않 · 알 수 없 … |
| 양보 | `REFUSAL_CONCESSIONS` (1) | "…있지만/…보이지만" — **거절이 아니다** |

**첫 문장만 본다. 이것이 가장 중요한 조임쇠다.** 프롬프트가 "근거가 없으면 … 한 문장으로
밝히라"고 요구하므로 진짜 거절은 답변을 열면서 나온다. 실측에서도 거절 질의 24건 전부가
첫 문장에서 거절했다. 본문 어디든 보면 **근거를 대고 나서 덧붙인 단서**까지 거절로 잡힌다.

### 방어선을 세우다가 3번 요구를 하나 바꿨다

원래 요구는 "정답이 있는 행이 **하나라도** 거절로 잡히면 실패"였다. 그대로 두면
**채점기를 틀리게 만들어야** 만족된다 — 검색이 못 찾아 모델이 **실제로 거절한** 답변이
정답 있는 질의에도 있기 때문이다(`ko_04`·`ko_05`·`py_ko_07`·`py_id_05`·`py_id_06`).
그것을 "거절이 아니다"라고 재는 자가 오히려 고장 난 자다.

그래서 방어선을 **"정답 파일을 짚은 답변(`_cited`)이 거절로도 잡히면 실패"** 로 바꿨다.
막으려던 것("지어낸 답 + 면피 한 줄"이 거절로 통과)은 그대로 막으면서, 자를 틀리게
만들지 않는다. 그리고 이 조건은 실제로 **한 건을 잡아냈다**:

    [apns4j/rag] ap_ko_06
    검색된 코드 범위에서는 실패 알림을 캐시하는 부분(`ApnsChannel.java`)은 보이지만,
    재전송을 수행하는 로직 자체는 확인되지 않습니다.

`ApnsChannel.java` 를 정확히 짚으면서 일부만 없다고 한 답변이다. 여기서 양보 규칙이
나왔다 — 데이터에 맞춰 깎은 것이 아니라, **방어선이 구조적 빈틈을 가리켰다.**
모호한 문장은 거절로 세지 않는다. 적게 세는 쪽으로 틀려야 tool use 가 실제보다
정직해 보이지 않는다.

숫자로 정리하면 이번 측정 89건 = 거절 질의 24 + 정답 있는 질의 **65** 다
(과제 문안의 74 는 계산 착오다). 거절 24건 중 옛 자로 통과한 것이 9, 실패가 15였다.

### 새 기준선 — **거절 축만** 옛 수치와 비교 불가

| 세트/팔 | 인용 (그대로) | 거절 (옛 → 새) |
|---|---|---|
| air / rag | 0.7059 | 0.6667 → **1.0** |
| marryday / rag | 0.6875 | 0.6667 → **1.0** |
| apns4j / rag | 0.7500 | 0.0 → **1.0** |
| apns4j / full | 0.9375 | 0.1667 → **1.0** |

**인용 정확도는 한 자리도 바뀌지 않았다** — `_cited()` 를 안 고쳤고, 재채점이 그것을
확인해 준다. 비용·지연도 그대로다(같은 응답을 다시 읽었을 뿐이다).

**거절 축은 자가 바뀌었으므로 옛 수치와 비교하지 말 것.** 특히 `apns4j/rag` 의 0.0 은
바닥 효과였다 — 6건 다 지어낸 것이 아니라 6건 다 규정 표현을 안 쓴 것이었다.
**1단계에서 이 숫자가 오르면 tool use 의 성과가 아니라 자를 고친 결과다.**

### 이제 거절 축은 24/24 다 — 헤드룸이 아니라 회귀 감시선이다

새 자로는 네 팔 모두 1.0 이라 더 올라갈 곳이 없다. 이건 결함이 아니라 이 축의 성격이다 —
판정 기준 B 는 "동률이면 거절 정확도를 본다. **떨어지면** 기각"이므로, 이 축은 처음부터
**떨어지는 것을 잡는 감시선**이다. tool use 가 검색을 반복하다 지어내기 시작하면
1.0 에서 내려온다.

### 날조 0/15 — tool use 이전의 기준선

이 결과 자체를 남긴다. 옛 자로 실패한 15건을 사람이 전부 읽었고, **모두 모델이 "없다"고
정확히 답했다.** 한 건도 지어내지 않았다. 1단계에서 이 값이 유지되는지가 tool use 도입의
핵심 질문이다 — 검색을 반복하는 경로는 계속 뒤지다 지어낼 여지가 더 크기 때문이다.

### 부재 판정 규칙을 평가셋에 박았다

`ap_ab_02` 에서 낱말 0회 검색만으로 부재를 판정했다가 근거가 틀렸다. 판정은 맞았지만
같은 방식으로 질의를 더 넣으면 다음엔 판정도 틀린다. `search_eval_dataset.py` 주석에
규칙을 넣고 `ap_ab_02` 를 **반례로 명시**했다 — 기능 이름뿐 아니라 그 기능이 쓰는
식별자·상수·주소까지 함께 본다.

### 검증

- **342 passed / 14 skipped** · ruff(E9,F) 통과 · LLM 호출 0회
- 재채점 4건 통과: 거절 24/24 검출 · 정답 인용과의 충돌 0건 · 첫 문장 제한 확인 · 기준선 출력
- 변이 4건 전부 의도한 테스트를 깨뜨렸다 (첫 문장 제한 제거 · 양보 규칙 제거 ·
  전체 주입 계열 제거 · 부정 술어 요구 제거). 바이트로 확인하고 복원 후 일치
- 로그가 없는 환경에서도 자가 지켜지도록, 실제 답변의 첫 문장을
  `test_citation_scoring.py` 에 고정했다(`Back/logs/` 는 `.gitignore` 대상이다)

## 1단계 착수 전 조사 — 소스가 없다는 것을 확인했다 - 2026-08-22

1단계(하이브리드 tool use)를 설계하기 전에 "무엇이 가능한가"만 봤다. 코드는 한 줄도
고치지 않았다. **조사가 1단계를 세 군데 바꿨다.**

### 결론부터 — 도구가 서빙할 원문이 어디에도 없다

`fetch_source_files()` 로 받은 `{경로: 내용}` 은 메모리에만 산다. 종착지가 셋인데
(정적분석 집계 · 청킹·임베딩 · 전체 주입 번들) **원문이 남는 것은 마지막 하나뿐**이다.

그 하나가 `repo_snapshots.source_bundle` 인데 `FULL_INJECTION_MAX_TOKENS`(57,000)
이하일 때만 채워진다. **소스가 이미 프롬프트에 통째로 들어가 도구가 필요 없는 저장소에만
원문이 있고, 도구가 절실한 큰 저장소에는 없다.** 이 역설이 곧 다음 절 작업의 이유다.

### 청크로는 복원할 수 없다 — 추정이 아니라 실측이다

STATUS.md §4 는 "`_merge_small` 이 떨어진 조각을 이어붙여 줄이 어긋난다"고만 적어
두었다. 실제로 재 보니 **어긋나는 것은 줄 번호가 아니라 내용 자체였다.**
로컬 소스 캐시 3개를 `chunk_file()` 에 그대로 통과시켰다(LLM·GitHub·임베딩 호출 0회):

| 저장소 성격 | 파일 | 청크 | 완전복원 가능 파일 | 사라진 비어있지 않은 줄 | content 줄수 ≠ end−start+1 |
|---|---|---|---|---|---|
| 작은 Java 라이브러리 | 32 | 168 | **0 / 32** | **11.1%** (233/2,096) | **48.8%** |
| 중간 Java 웹앱 | 47 | 236 | 6 / 47 | **12.7%** (407/3,207) | 33.9% |
| 큰 Python/JS | 181 | 4,364 | 59 / 181 | **8.5%** (5,153/60,730) | 13.8% |

**48.8% 가 STATUS.md 의 수치와 정확히 일치했다 — 그 측정이 이 저장소였고, 검산이 된다.**

**이 값들은 하한이다.** 이 실행은 `_split_by_tokens()` 를 태우지 않았다(임베딩 모델
미설치 환경). 프로덕션은 거기서 한 번 더 자르고 40자 미만 부스러기를 또 버린다.

### 줄이 사라지는 코드상 위치 네 곳

- `chunker.chunk_file:264` — `IMPORT_NODES` 최상위 자식을 **통째로 건너뛴다.**
  import 줄은 인덱스에 애초에 존재하지 않는다
- `chunker._node_chunks:194-206` — 컨테이너는 `헤더(start→inner[0].start)` + `inner 정의들`
  만 낸다. **inner 정의 사이의 틈**(필드·주석·빈 줄)과 마지막 정의 뒤 꼬리는 어느 청크에도
  안 들어간다
- `chunker._merge_small:236` — `len(content.strip()) < 40` 인 청크를 **버린다**
- `chunker._merge_small:228` — 떨어진 두 조각을 `"\n"` 으로 붙이고 `end_line` 만 뒤쪽
  것으로 바꾼다. 사이 줄은 없는데 범위는 이어진 것처럼 보인다 — **48.8% 의 정체**

반대로 `_line_chunks` 의 `OVERLAP_LINES = 3` 은 같은 줄을 **중복**시킨다.
청크 집합은 원본의 부분집합도 아니고 상위집합도 아니다. **이어붙이기로 복원이 안 될 뿐
아니라, 복원했다고 착각하면 조용히 틀린다.**

### 캐시 접두사는 도구 호출에 견딘다 — 새는 곳은 꼬리다

`cache_control` 은 정확히 **2개**다(상한 4개 중): `system` 블록과 `messages[0]`
(스냅샷 컨텍스트 + 있으면 번들). 렌더 순서가 `tools` → `system` → `messages` 이므로

- **`tools` 를 추가하는 것**은 위치 0 이라 두 브레이크포인트를 한 번 무효화한다.
  배포 시점 1회이고, 도구 목록이 요청마다 같기만 하면 그 뒤로는 새 접두사로 안정된다.
  → **도구 목록을 저장소마다 다르게 하지 말 것.** 조건부 시스템 섹션과 같은 부류의
  조용한 무효화다
- **라운드트립이 쌓이는 것**은 브레이크포인트 **뒤**에 붙으므로 접두사를 깨지 않는다.
  `messages[0]` 의 브레이크포인트는 자기 앞에 system 밖에 없어 20블록 되돌아보기 창에도
  안 걸린다

**즉 캐시는 견딘다. 새는 곳은 브레이크포인트 뒤의 꼬리다** — 지금 거기에 `history` +
`snippets` + `question` 이 통째로 들어 있고, 라운드트립마다 정가로 다시 청구된다.

### 비용 식과 검산

P = 캐시 접두사, H = 이력, S = 미리 넣은 스니펫, q = 질문, aᵢ·rᵢ = i번째 호출의
출력·tool_result. 호출 N회(도구 N−1회 + 최종 답변 1회):

    입력 청구 = N·(0.1·P + H + S + q) + Σᵢ₌₁^{N−1} (N−i)·(aᵢ + rᵢ)
    출력 청구 = Σ aᵢ

앞항이 N에 선형, 뒷항이 **N에 이차**다. 기준선은 N=1 인 같은 식이다.

**식을 기준선으로 검산했다.** air/rag 기록(P=1,140 · H=0 · S+q≈2,300 · a≈330, 도입가 $2/$10):

    (0.1×1140 + 2300)×$2/1M + 330×$10/1M = $0.008128

기록된 `avg_cost_usd` 는 **$0.008155**. 식이 맞는다.

### 하이브리드가 순수 tool use 보다 비싸다

도구 턴 a=150, tool_result r=1,500 을 대입한 값. `COST_RATIO_LIMIT = 3.0` →
질문당 상한 $0.024465.

| 라운드트립 N | 하이브리드(스니펫 유지) | 순수 tool use(스니펫 제거) |
|---|---|---|
| 1 (기준선) | $0.00813 · 1.00× | — |
| 2 | $0.01776 · 2.18× | $0.00776 · 0.95× |
| 3 | $0.03068 · **3.78× ← C 위반** | $0.01712 · 2.11× |
| 4 | $0.04691 · 5.77× | $0.02883 · **3.55× ← C 위반** |

**미리 넣은 스니펫 2,300 토큰이 브레이크포인트 뒤에 있어서 라운드트립마다 정가로
되풀이 청구되기 때문이다.** "스니펫도 주고 도구도 준다"는 안전해 보이지만 비용 축에서는
가장 나쁜 조합이다.

그리고 **`read_file` 이 파일을 통째로 돌려주면 즉사한다** — r=4,000(400줄)으로 바꾸면
하이브리드 N=3 이 5.6배가 된다. 범위 인자는 편의가 아니라 **비용 상한 장치**다.

### `MAX_HISTORY_MESSAGES = 20` 과 tool_result

지금 구조에서는 tool_result 가 이력으로 세어지지 **않는다** — `chats.add_exchange()` 는
질문·답변 2행만 넣고, tool_use/tool_result 는 한 요청 안에서만 산다.

남기기로 하면 셋이 동시에 깨진다: (1) `list_messages(limit=20)` 은 **메시지 개수로**
자르므로 경계가 `tool_use`/`tool_result` 사이에 떨어지면 짝 잃은 블록이 남아 API 가 400 을
낸다, (2) `build_chat_messages` 가 이력을 `{role, content: 문자열}` 로 넘긴다,
(3) `messages.content` 가 `TEXT` 다. **남기지 않는 쪽으로 간다.**

### eval_sources 캐시를 승격하지 않는다

`tests/test_search_quality.py::_source_files()` 가 쓰는 `Back/cache/eval_sources/` 는
프로덕션 코드가 한 줄도 참조하지 않고(`app/` 전수 grep 히트 0), 무엇보다 **키가
`(owner, name)` 이라 스냅샷 단위가 아니다.** 저장소가 갱신돼도 같은 파일을 덮어쓴다 —
세션이 보는 코드 버전을 스냅샷으로 고정한다는 전제와 정면으로 어긋난다.
테스트 전용으로 그대로 둔다.

### 1단계에 미리 잡아 둘 것

- **프롬프트를 고치는 순간 인용 기준선 89회가 무효가 된다** ($0.89, 2026-08-31 이후 $1.33).
  `CHAT_SYSTEM_PROMPT:130-136` 이 코드가 오는 형태를 둘로 못 박아 두었고 `:143-148` 이
  행 번호 규칙을 그 둘에 매핑한다. 도구 결과가 들어오면 둘 다 고쳐야 한다.
  특히 `:145-146`("조각 안에서 특정 줄을 세어 짚지 마세요")은 적용 대상을
  `format_snippets` 조각으로 **좁혀야** 한다 — 안 그러면 모델이 가장 믿을 만한 근거를
  못 믿는다. `read_file` 이 `context_builder.number_lines()` 를 쓰면 전체 주입 번들과
  형식이 같아지므로 **형식은 셋이 아니라 둘로 유지된다**
- **`REFUSAL_SCOPES` 5개에 구멍이 있다.** tool use 답변은 "읽어 본 파일에는 없습니다" ·
  "세 파일을 열어 봤지만 없습니다" 로 나오는데 어느 것도 안 걸린다. 거절 축은
  "떨어지는 것을 잡는 감시선"인데, 이대로면 **정직하게 답했는데도 축이 내려가** 판정 기준 B 가
  tool use 를 틀리게 기각한다. 기준선 재측정과 **같은 커밋**이어야 한다

## 소스 보관 — 스냅샷 단위로 원문을 남긴다 - 2026-08-22

브랜치 `feat/snapshot-sources`. **1단계를 착수하지 않는다.** 위 조사대로 도구가 서빙할
원문이 없어서, 그대로 가면 `search_code` 하나짜리 도구가 된다 — 그건 지금 코드가 이미
하는 일이라 잴 것이 없다.

이 작업의 성공은 하나다: **임의의 스냅샷에서 임의의 파일 원문을 GitHub 재요청 없이
바이트 단위로 꺼낼 수 있다.** `read_file`·`grep`·도구 루프는 포함하지 않는다.

### 계획

- [x] `snapshot_source_files` 테이블 (`schema.sql`, 멱등)
      → verify: `init_schema()` 를 두 번 불러도 성공
- [x] `config.MAX_STORED_SOURCE_BYTES` + `.env.example`
      → verify: `tests/test_config.py` 가 양방향으로 대조
- [x] `app/db/sources.py` — `put_files` / `get_file` / `list_paths` / `count`
      → verify: `tests/test_db_sources.py`
- [x] `indexer._store_sources()` 를 `run_build` 에 배선 (**전체 주입 판정보다 앞**)
      → verify: `tests/test_indexer.py`
- [x] STATUS.md §1.1·§2.2·§4 등재 → verify: `tests/test_status_doc.py`

### 정한 것과 그 이유

**별도 테이블이다. `repo_snapshots` 에 열을 붙이지 않았다.** 처음에는 JSONB 열을 생각했는데
(`repo_snapshots` 에 두라는 지시와도 맞는다), 다음 단계의 소비자가 `read_file` 과 `grep`
이라는 것이 갈랐다. JSONB 는 TOAST 라 `source_files -> '경로'` 한 번이 수 MB 짜리 값
전체를 풀고, `grep` 은 SQL 로 아예 못 해서 전부 파이썬으로 끌어와야 한다.
PK `(snapshot_id, path)` 면 파일 하나를 한 행으로 읽고 `grep` 은 DB 가 훑는다.

**`source_bundle` 과 합치지 않았다.** 그쪽은 프롬프트에 넣으려고 `12|코드` 로 렌더한
결과물 한 덩어리이고 이쪽은 파일 단위 원본이다. 합치면 둘 중 하나는 반드시 거짓이 된다.

**보관은 `run_build` 한 곳에서, 전체 주입 판정보다 앞에서 한다.** `run_build` 는 `files` 를
**항상** 갖는 유일한 지점이다 — 넘겨받았든(`/analyze` 의 정적분석) 스스로 받았든
(handoff 상한 초과 · 캐시 히트 · `/admin/api/rebuild-index` · 세션만 복원한 첫 질문).
`/analyze` 에 넣으면 캐시 히트 경로(`_start_indexing(cached, ..., files=None)`)와
`MAX_HANDOFF_CHARS` 초과 경로가 통째로 빠진다. 그리고 **전체 주입 분기보다 뒤에 두면
작은 저장소가 또 빠져 나가 위 역설이 그대로 남는다.**

**크기 상한을 넘으면 한 행도 넣지 않는다. 자르지 않는다.** 수집 상한이 3,000 × 200KB 라
이론상 600MB 다(실측은 76KB / 131KB / 2.5MB). 잘라서 일부만 보관하면 도구의 "없습니다"가
**"저장소에 없다"인지 "잘려서 없다"인지 구분되지 않는다.** 그건 거절 축(`ABSENT_SETS`)이
재는 바로 그 문장을 오염시킨다 — 조용히 반쪽짜리 근거를 주느니 그 스냅샷에서는 도구를
아예 못 쓰게 두는 쪽이 맞다. `MAX_STORED_SOURCE_BYTES = 20MB` 는 `MAX_HANDOFF_CHARS` 와
같은 근거·같은 크기다.

**상태 열을 두지 않았다.** "보관됐는가"는 `sources.count() > 0` 으로 답하고 "왜 안 됐는가"는
로그가 답한다. 열을 하나 더 두면 그 값과 실제 행 수가 어긋날 자리가 생긴다.

**`put_files` 는 제자리 교체다** (한 트랜잭션에서 `DELETE` 후 `INSERT`). 청크가 빌드를
쌓고 포인터를 옮기는 것과 다른 이유는, 여기 담기는 것이 빌드마다 달라지지 않기 때문이다 —
같은 스냅샷은 같은 시점의 저장소라 재색인해도 같은 소스가 들어온다. upsert 로 하면
**파일이 줄어든 경우 옛 행이 남아 도구가 저장소에 없는 파일을 읽어 준다.**

### 계약 — 결함이 아니다

**도구가 서빙할 수 있는 범위 = 색인이 본 파일 집합.** `fetch_source_files()` 가 이미 거른
것은 보관해도 영영 없다: `language_for()` 가 모르는 확장자 · 200KB 초과 · 바이너리 ·
`SKIP_DIR_PARTS` 15개 · 3,000개 상한(tarball 순서로 끊긴다).

**"저장소에 없다"와 "수집 범위에 없다"는 다른 말이고, 섞이면 거절 축이 오염된다.**
STATUS.md §2.2 에 적었다. 프롬프트 반영은 1단계로 미룬다 — 프롬프트를 고치면 기준선
89회가 무효가 되므로 한 번에 몰아서 한다.

### 검증

- **357 passed / 14 skipped** (기준선 342 → 신규 15건). **무과금** — LLM·GitHub·임베딩
  호출 0회 (테스트는 `fetch_source_files` 를 대역으로 갈고, 청킹·임베딩도 기존
  `fake_sources` 픽스처를 쓴다)
- `tests/test_db_sources.py` 9건 — 바이트 동일(한국어 주석·CRLF·탭·U+FFFD·끝 개행 없는
  파일) · 스냅샷 분리 · 제자리 교체 · CASCADE · `cleanup.run(apply=True)` 경로
- `tests/test_indexer.py` 6건 추가 — **전체 주입 경로에서도 보관된다**(이 작업 전체의
  이유다) · RAG 경로에서도 보관된다 · 캡 초과 시 정확히 0행 · 0 은 제한 없음 ·
  보관이 실패해도 색인은 성공 · 재색인이 옛 행을 남기지 않는다

### 변이 확인 — 하나가 안 잡혀서 테스트를 고쳤다

워킹트리 사본을 뜨고 sha256 으로 복원을 확인했다(`git show` 를 쓰지 않는다 —
`core.autocrlf` 때문에 blob 과 워킹트리의 바이트가 다르다). 셋 다 복원 후 해시 일치.

| 변이 | 깨진 테스트 |
|---|---|
| `_store_sources` 호출을 전체 주입 판정 **뒤로** 옮김 | `test_full_injection_path_still_stores_the_source` |
| `put_files` 의 `DELETE` 제거(= upsert) | `test_reput_replaces_in_place` · `test_reput_does_not_touch_other_snapshots` · `test_reindexing_replaces_the_stored_source` |
| 캡 초과 시 **잘라서** 일부만 보관 | (처음엔 **안 잡혔다** → 아래) |

**세 번째가 통과했다.** 대역 저장소의 파일이 하나뿐이라 "12바이트까지만 자른다"와
"전부 포기한다"가 **같은 결과(0행)** 를 냈다. 자를 것이 없으면 자르기와 포기가 구분되지
않는다 — 테스트가 캡의 *존재*만 재고 있었지 **정책**을 재고 있지 않았다.

파일 셋(각 6바이트)에 상한 12바이트로 바꿔서, 자르면 2행이 남고 포기하면 0행이 되도록
갈랐다. 그 뒤 같은 변이가 실제로 깨졌다. **이 절의 핵심 결정("자르지 않는다")을 재는
테스트가 그때까지 아무것도 재지 않고 있었다.**

### 함정: `indexer.py` 의 죽은 import 는 원래 있던 것이다

`ruff check app tests --select E9,F` 가 `indexer.py` 의
`from app.core.chunk_rule import rule_version` 를 F401 로 잡는다. **이번 작업이 만든 것이
아니다** — `git show HEAD:` 로 뽑은 사본에도 같은 경고가 뜬다(`index_queue.py:69` 가 자기
안에서 따로 import 한다). 07 절들의 "ruff 통과" 기록과 어긋나므로 여기 남긴다.
CLAUDE.md §3 대로 지우지 않았다. (→ 아래 '죽은 import 정리' 절에서 지웠다)

## 죽은 import 정리 — `rule_version` 을 지웠다 - 2026-08-22

브랜치 `chore/dead-code`. 위 절이 남겨 둔 `indexer.py` 의
`from app.core.chunk_rule import rule_version` 하나만 지운다.

### 지우기 전에 확인한 것 — ruff 의 F401 만 믿지 않았다

이 저장소에는 **심볼을 정적/동적으로 읽는 코드가 셋** 있어서, "참조가 없다"를 린터
하나로 판정하면 안 된다. 전수로 확인했다:

| 읽는 쪽 | 무엇을 읽는가 | `indexer.rule_version` 에 닿는가 |
|---|---|---|
| `chunk_rule._normalized_source` | `_RULE_FUNCTIONS` 6개의 `inspect.getsource` → AST | 아니다 — 전부 `chunker` 의 함수다 |
| `chunk_rule._constants` | `chunker` 모듈 속성 + `config` 두 개 | 아니다 |
| `test_status_doc._actual` | STATUS.md 표가 가리킨 심볼을 `getattr(import_module(...))` | 아니다 — §1.4 의 `app.services.indexer` 행은 `TOP_K`·`MAX_HANDOFF_CHARS`·`LOW_INFO_PENALTY`·`CANDIDATE_MULTIPLIER`·`STYLE_LANGUAGES`·`ENTRY_POINT_MARKERS`·`INHERITANCE_MARKERS` 뿐이다 |

`test_status_doc._config_defaults`·`_config_env_keys` 도 AST 를 쓰지만 `app/config.py`
한 파일만 파싱한다. `getattr`·`importlib`·`__dict__`·`vars()`·`inspect.getsource`·
`ast.parse` 를 `Back/**/*.py` 전수 검색해 위 다섯 곳이 전부임을 확인했다.

전 저장소(`.md`·`.sql` 포함) 검색에서도 `rule_version` 소비자는 전부
`from app.core.chunk_rule import ...` 로 직접 가져간다 — `indexer` 를 경유하는 곳은 없다.

### 진짜 위험은 F401 이 아니라 해시였다

`rule_version()` 이 달라지면 **모든 활성 색인이 낡은 것으로 뜬다.** 이 함수의 재료가
청킹 함수의 AST 라서, indexer 를 건드리는 것이 해시에 닿는지부터 실측했다:

    지우기 전  822bb217
    지운 후    822bb217   (동일 — 07 로그가 기록한 현행 규칙 그대로)

논증으로 끝내지 않고 잰 이유는, 틀렸을 때의 대가가 전체 재색인이기 때문이다.

### 검증

- `ruff check app tests --select E9,F` — **All checks passed** (이 저장소에서 처음으로 0건)
- **358 passed / 13 skipped** — 지우기 전과 같은 수. 줄어든 테스트 없음
- 서버 기동: `uvicorn app.main:app --port 8123` → `Application startup complete`,
  `GET /health` 200 `{"status":"ok"}`
- 기동 경로 직접 확인: `main._log_stale_indexes()` 가
  `청킹 규칙 822bb217 — 낡은 색인 없음` 을 낸다. **`main.py:25` 가 `rule_version()` 을
  쓰는 바로 그 함수라, 지운 import 와 가장 가까운 경로다**
- `hasattr(indexer, "rule_version")` → `False`

### STATUS.md §4 에는 이 항목이 없었다

작업 지시는 "§4 에서 해당 줄을 제거하라"였는데 §4(열린 과제)에 그 줄은 처음부터 없다.
기록은 `TROUBLESHOOTING.md` 의 '테스트 · 검증 · 도구' 절과 위 '함정' 절 두 곳에만 있었다.
`TROUBLESHOOTING.md` 의 행은 **그대로 둔다** — 그 행이 남기는 것은 이 import 의 존재가
아니라 "린터 경고가 내 것인지 `git show HEAD:` 로 먼저 가른다"는 방법이고, 그건 여전히 유효하다.

## 1단계: 순수 tool use — 구현과 무과금 검증 (측정 전 정지) - 2026-08-22

브랜치 `feat/tool-use`. **하이브리드가 아니라 순수 tool use 다** — 스니펫 사전 주입을 빼고
`search_code` 를 도구로 내렸다. 근거는 위 조사의 비용표다: 미리 넣은 스니펫은 캐시
브레이크포인트 **뒤**라 라운드트립마다 정가로 되풀이 청구되고, 3회에 3.78배가 되어
판정 기준 C(3배)를 넘는다. 도구만 쓰면 같은 3회가 2.11배다.

### 도구는 셋. 더 만들지 않았다

`Back/app/services/tools.py` 가 스키마와 **스냅샷에 묶인 실행기**를 만든다.
`claude_client` 는 DB·검색을 모른다 — LLM 어댑터로 남겼고, `api/chat.py` 가 둘을 잇는다.

| 도구 | 감싼 것 |
|---|---|
| `search_code(query)` | `indexer.search_code` → `indexer.format_snippets` |
| `read_file(path, start_line, end_line)` | `sources.get_file` → `context_builder.number_lines` |
| `grep(pattern)` | `sources.grep` (신규) |

**`start_line`·`end_line` 을 `required` 에 넣었다.** 편의 인자가 아니라 비용 상한
장치다 — 선택으로 두면 모델이 파일을 통째로 부르고, r=4,000 이면 3회에 5.6배가 된다.

**`grep` 은 보관 소스 위에서 돈다.** `chunks.content` 에 걸면 비어있지 않은 줄의
8.5~12.7% 가 빠진 코퍼스를 뒤지게 되어 "없다"가 거짓이 된다. 줄 가르기는 DB 가 한다
(`unnest(string_to_array(content, E'\n')) WITH ORDINALITY`) — 파이썬으로 끌어오면
매칭 없는 파일까지 전부 전송된다.

### grep 에서 두 가지를 놓쳤다가 고쳤다

- **CRLF.** `\n` 으로 가르면 모든 줄이 `\r` 로 끝나 인용이 한 글자씩 어긋난다.
  `rtrim(text, E'\r')` 로 뗀다.
- **LIKE 와일드카드.** `_` 가 와일드카드라 `find_user` 가 `findXuser` 에도 걸린다.
  모델이 "있다"고 본 것이 실제로는 없는 이름이 되므로 이스케이프한다.
  정규식(`~*`)을 안 쓴 이유도 같은 계열이다 — 모델이 지어낸 패턴이 백트래킹으로 DB 를 문다.

### 상한 셋 — 산식에서 나온 값이다

| 상수 | 값 |
|---|---|
| `claude_client.MAX_TOOL_ROUND_TRIPS` | 3 |
| `tools.MAX_TOOL_RESULT_TOKENS` | 800 |
| `tools.MAX_GREP_MATCHES` | 30 |

`P=1,140 · q≈40 · a_tool=150 · a_final=330 · r=800` 을 조사의 산식에 대입하면

    R=3 (호출 4회)  $0.020432  →  2.51배  ✓
    R=4 (호출 5회)  $0.029840  →  3.66배  ✗

> **여기 `r=800` 과 위 조사표의 `r=1,500` 은 모순이 아니라 다른 양이다.**
> 실행 전 점검에서 이 둘이 섞여 있는 것이 드러나 갈라 적는다.
>
> | | 무엇 | 무엇을 정했나 |
> |---|---|---|
> | `r=1,500` (위 '하이브리드가 순수 tool use 보다 비싸다') | **가정.** 캡이 생기기 전 조사가 잡은 전형적 결과 크기 | 3.78×/2.11× → **순수 tool use 로 간 근거** |
> | `r=800` (여기) | **상한.** `MAX_TOOL_RESULT_TOKENS`. 모든 결과가 캡에 닿는 최악 | 2.51× → **`MAX_TOOL_ROUND_TRIPS=3`** |
>
> **실측이 대조할 상대는 가정(1,500)이지 상한(800)이 아니다.** 평균이 800 아래인 것은
> 캡이 있으니 당연하고 아무것도 증명하지 않는다. 그래서 기록의 `result_tokens` 는
> **자르기 전** 크기이고(캡이 무엇을 막았는지), 실제로 간 크기는 `sent_chars` 로 따로
> 남긴다. 청구된 토큰의 정본은 이 둘이 아니라 호출별 `input_tokens` 다.

**3 이 3.0 아래에 남는 마지막 값이다.** 남는 0.49배는 effort 가 만드는 사고 토큰
(출력으로 청구된다)이 `a_tool` 추정을 넘길 여지를 덮는 몫이다.
**셋 중 하나만 고치면 이 산식이 깨진다** — STATUS.md §1.5 에 근거째로 적었다.

절단은 **문자 수로 먼저 거르고 토큰으로 판정한다.** `estimate_tokens` 는 2.0자/토큰이라
코드에서는 실제보다 많게 잡으므로, 추정이 상한 안이면 실제도 안이다 — 그때는 토큰 계산
왕복을 아낀다(`_try_full_injection` 이 바이트로 먼저 거르는 것과 같은 구조).
자른 경우 **잘렸다는 문구를 결과 안에 넣는다** — 안 넣으면 모델이 잘린 것을 "없다"로 읽는다.

### 한도에 닿았을 때 `tool_choice` 를 쓰지 않았다

`tool_choice` 변경은 tools·system 캐시는 살리지만 **messages 캐시를 무효화**한다.
그러면 마지막 호출이 스냅샷 접두사를 통째로 정가로 다시 계산한다 — 접두사가 큰 저장소일수록
손해가 크다. 대신 *"도구 호출 한도에 도달했습니다…"* 를 **마지막 사용자 메시지 꼬리에**
덧붙인다. 꼬리는 브레이크포인트 뒤라 접두사를 건드리지 않는다.

### 한 질문이 호출 여럿이 됐다

`_call()` 은 1회 호출 전용이라 그대로 두고(요약이 쓴다) `_call_loop()` 을 더했다.
`_raw_call()` 로 파라미터 조립을 한 곳에 모았다.

- 반환 dict 는 `_call()` 과 **같은 키 집합 + `round_trips`·`hit_round_trip_cap`** 이다.
  `run_log.append_run()` 과 `billable_tokens()` 가 그대로 재사용된다
- 토큰 넷과 `llm_ms` 는 **전 호출의 합**이다. 마지막 호출만 세면
  `rate_limit.record_tokens(billable_tokens(result))` 가 사실상 꺼진다 — 테스트로 못 박았다
- 응답 블록을 **그대로** 되돌린다. 텍스트만 뽑으면 `tool_use` id 가 사라져 다음 요청이 거부된다
- 결과는 **한 user 메시지에 모아** 보낸다. 나눠 보내면 모델이 병렬 호출을 그만둔다
- 실패한 도구도 `is_error: true` 로 돌려준다. 빠뜨리면 짝 없는 `tool_use` 로 API 가 거부한다

`round_trips` 를 `runs` 테이블에 남겼다(`FIELDS` 15→16). **실사용의 라운드트립 분포는
여기서만 나온다** — 하네스는 평가셋 질의만 본다. `FULL_INJECTION_MAX_TOKENS` 를 4세션
표본으로 역산해 두고 아직 다시 못 뽑은 것과 같은 실수를 되풀이하지 않으려는 열이다.

### 도구를 안 붙이는 경우는 둘뿐이다

전체 주입 스냅샷(소스가 이미 접두사에 다 들어가 있다)과 색인이 아직 안 끝난 스냅샷.
**둘 다 스냅샷 속성이고 한 대화 안에서 바뀌지 않는다** — 저장소 이름이나 요청 내용으로
가르지 않는다. 계획 문안은 "도구 목록은 항상 같다"였는데, 전체 주입 팔은 기준선에서
`tools` 없이 측정한 모양이라 프로덕션이 달라지면 그 측정이 무효가 된다. 그래서 갈랐다.

**보관된 소스가 없는 스냅샷은 도구를 그대로 받는다.** 배포 직후에는 모든 옛 스냅샷이
여기 해당한다(소스 보관이 그 뒤에 생겼고 재색인은 수동이다). `read_file`·`grep` 이
"보관된 소스가 없습니다 — search_code 를 쓰세요"로 답하고 모델이 선회한다.

### 프롬프트는 두 군데만 고쳤다

- 코드가 오는 경로에 도구 셋을 추가했다. `read_file` 은 `number_lines()` 를 재사용하므로
  전체 주입 번들과 **같은** `{n}|{line}` 형식이다 — **셋째 형식이 아니다**
- "조각 안에서 특정 줄을 세어 짚지 마세요"의 적용 대상을 **`search_code` 결과로 좁혔다.**
  안 좁히면 모델이 가장 믿을 만한 근거(원본에서 직접 잘라 온 `read_file` 결과)를 못 믿는다
- **§A 계약**을 넣었다 — 도구가 보는 범위는 색인이 수집한 파일 집합이고,
  "저장소에 없다"와 "수집 범위에 없다"는 다른 말이다

`REFUSAL_PHRASES` 두 개는 리터럴로 남겨 정합성 검사가 계속 깨질 수 있게 뒀다.

### 거절 채점기 구멍을 같은 커밋에서 메웠다

`REFUSAL_SCOPES` 에 `(?:읽어|열어|확인해|찾아|살펴)\s*본` 을 더했다(5→6).
도구 경로는 "검색된 범위" 대신 **자기가 한 행동**으로 스코프를 말한다.

**기존 89건으로 재채점했다 — 무과금, API 호출 0회.** 거절 24/24 유지, 인용 정확도
네 팔 모두 한 자리도 안 변함, 정답 인용과의 충돌 0건.

다만 **89건에는 도구 경로 답변이 없어서 재채점으로는 새 계열이 동작하는지 증명되지
않는다.** 그래서 `test_citation_scoring.py` 에 세 건을 고정했다 — 잡아야 할 문장,
**부정 술어 없이 행동만 말한 문장은 거절이 아니라는 것**(이쪽이 위험하다. 도구를 쓰면
"읽어 본 파일에서 확인했습니다" 처럼 행동을 먼저 말하고 답하는 형태가 흔하다),
그리고 양보 규칙이 도구 경로에도 걸린다는 것.

### 하네스에 `tool` 팔을 더했다

`_arms_for()` 가 이제 `(rag, full, tool)` 또는 `(rag, tool)` 을 준다. **`rag` 를 같은
실행에서 다시 재야 한다** — 프롬프트를 고쳤으므로 옛 인용 기준선이 무효다.
하네스는 **프로덕션과 같은 실행기**(`tools.build_executor`)를 쓴다. 자기 대역을 쓰면
서비스가 아니라 하네스를 재게 된다.

보고서에 `질문당 도구호출`·`한도에 닿음`·`RAG 대비 배수`를 넣었다. 배수를 눈으로 나누게
두면 틀리므로 판정 기준 C 를 코드가 적용해 출력한다. 판정 줄도 A→B→C 순서로 자동 출력한다
(단정하지는 않는다 — 결정은 사람이 한다).

과금 전에 **가짜 행으로 3팔·2팔 두 경로를 태워 봤다.** 포맷 오류 하나로 측정 한 판이
마지막 줄에서 죽는 것을 막으려는 것이다(0단계와 같은 이유).

### 검증 — 전부 무과금

- **410 passed / 13 skipped** (기준선 358 → 신규 52). LLM·GitHub·임베딩 호출 0회
- `ruff check app tests --select E9,F` **All checks passed** (0건 유지)
- 서버 기동 확인
- 변이 4건이 전부 의도한 테스트를 깨뜨렸다. 워킹트리 사본을 뜨고 sha256 으로 복원 확인:

| 변이 | 깨진 테스트 |
|---|---|
| 라운드트립 캡 제거 | `test_round_trip_cap_stops_the_loop` · `test_empty_answer_after_tools_is_not_blank` |
| usage 합산을 대입으로(`+=` → `=`) | `test_usage_is_summed_across_every_call` · `test_billable_tokens_counts_every_call` |
| `_truncate` 가 원문을 그대로 반환 | `test_long_result_is_cut_and_the_cut_is_stated` · `test_truncation_falls_back_when_tokens_cannot_be_counted` |
| grep 결과를 안 자름 | `test_grep_clips_at_the_match_cap_and_says_so` |

**대역은 두 동작이 다른 결과를 내게 짰다**(CLAUDE.md §4). grep 캡은 상한보다 10건 많은
매칭을 주어 "자름"과 "안 자름"이 갈리게 했고, 토큰 캡은 400줄에 상한의 4배를 물려
앞은 남고 뒤는 잘리는 것을 둘 다 단정했다 — 어제 이 대역 설계를 놓쳐 변이가 통과한 전례가 있다.

### 예상 비용 — 측정은 승인 후

세 세트 78질의(정답 65 + 거절 24 중 세트별 배분 그대로) × 팔 개수 =
apns4j 22×3 · air 23×2 · marryday 22×2 = **156 호출분**.

`rag`·`full` 은 지난 실비 그대로($0.0072~0.0096 / $0.01512). `tool` 은 라운드트립
분포를 R=1~3 균등으로 잡아 산식으로 뽑았다:

    R=1 (호출 2회)  $0.00732
    R=2 (호출 3회)  $0.01292
    R=3 (호출 4회)  $0.02043   ← 캡
    평균 ≈ $0.0136/질의

| | 도입가 (~8/31) | 정가 (9/1~) |
|---|---|---|
| 기준선 재측정 (rag·full 89회) | $0.89 | $1.33 |
| tool 팔 (67질의) | $0.91 (전건 캡이면 $1.37) | $1.37 (최악 $2.06) |
| **합계** | **≈ $1.80** (최악 $2.26) | **≈ $2.70** (최악 $3.39) |

**8/31 까지 9일 남았다.** 그 안에 재면 1.5배를 아낀다.

### 기록의 한계 — 무엇이 재현되고 무엇이 안 되는가

**`tool_result` 본문을 싣지 않았다.** 크기와 절단 여부면 산식에 충분하고 본문을 실으면
jsonl 이 소스만큼 커지기 때문인데, 그 대가로 **되살릴 수 있는 것과 없는 것이 갈린다.**

| 도구 | 결과를 되살릴 수 있나 | 왜 |
|---|---|---|
| `read_file` | **된다** | 기록에 경로와 범위가 있고 원문은 `snapshot_source_files` 에 스냅샷 단위로 보관돼 있다 |
| `grep` | 대체로 된다 | 패턴이 기록에 있고 같은 보관 소스 위에서 돈다. 재색인이 소스를 제자리 교체하지만 같은 스냅샷은 같은 커밋이라 내용이 같다 |
| `search_code` | **안 된다** | 결과가 그때의 **청크 빌드**에 달려 있는데 `KEEP_BUILDS = 1` 이라 재색인이 한 번 돌면 그 빌드의 청크가 지워진다. 청킹 규칙이 바뀌면 같은 질의도 다른 조각을 낸다 |

즉 **재색인이 한 번 돌면 이 측정의 `search_code` 결과는 영영 못 본다.** 나중에
"왜 재현이 안 되지"로 헤매지 않도록 여기 적어 둔다. 필요해지면 그때는 본문까지 실어야 한다.

### 측정 전에 하나 걸렸다 — 평가 스냅샷에 보관 소스가 0개였다

평가용 스냅샷 셋은 **소스 보관 기능보다 먼저** 색인됐고 재색인은 수동이라,
`sources.count()` 가 전부 0 이었다. 그대로 돌렸으면 `read_file`·`grep` 이 전부
"보관된 소스가 없습니다"를 돌려주고 **도구 팔이 `search_code` 하나짜리로 측정될 뻔했다** —
$1.80 을 쓰고 틀린 숫자를 사는 것이다.

하네스에 `_ensure_sources()` 를 더해 로컬 캐시(색인이 쓴 것과 같은 파일 집합)에서 채운다.
무과금이고 멱등이다. 이건 프로덕션의 옛 스냅샷이 겪는 상태와 같지만, **그 과도기를 재는
것은 이 측정의 목적이 아니다**(재색인으로 해소된다).

### 여기서 멈춘다

`-m "billed and evaluation"` 은 승인 없이 돌리지 않는다.

## 1단계 측정 — 실비 $2.1259, 그리고 거절 자가 또 샜다 - 2026-08-22

156 호출, **26분 42초, 실비 $2.1259** (예상 $1.80 대비 +18%, 최악 $2.26 안).
`air rag $0.2102 · tool $0.3705` · `marryday rag $0.2384 · tool $0.4745` ·
`apns4j rag $0.1846 · full $0.3506 · tool $0.2971`.

**판정하지 않는다.** 거절 축이 자의 구멍 때문에 새 있어서, 그것을 고친 뒤가 아니면
기준 B 를 댈 수 없다.

### 세트/팔

| 세트 | 팔 | 인용 | 거절(측정시) | 거절(재채점) | 질문당 비용 | 질문당 지연 | RAG 대비 |
|---|---|---|---|---|---|---|---|
| air | rag | 0.7647 | 1.0 | 1.0 | $0.009141 | 6,068ms | 1.00배 |
| air | tool | **1.0** | 0.1667 | **1.0** | $0.016109 | 10,909ms | 1.76배 |
| marryday | rag | 0.6875 | 0.8333 | 0.8333 | $0.010835 | 6,886ms | 1.00배 |
| marryday | tool | **0.75** | 0.1667 | **1.0** | $0.021569 | 12,609ms | 1.99배 |
| apns4j | rag | 0.8125 | 1.0 | 1.0 | $0.008392 | 5,980ms | 1.00배 |
| apns4j | full | **1.0** | 0.8333 | **1.0** | $0.015939 | 6,671ms | 1.90배 |
| apns4j | tool | **0.9375** | 0.5 | **0.6667** | $0.013505 | 9,762ms | 1.61배 |

지연은 도구 팔이 1.6~1.8배다(6초대 → 10~12.6초).

### 가정이 전부 과대였다

| | 가정 | air | marryday | apns4j |
|---|---|---|---|---|
| `r` (절단 전 결과 토큰) | 1,500 | 1,117.7 | 1,479.2 | 997.9 |
| `a` (도구턴 출력 토큰) | 150 | 124.2 | 138.8 | 118.6 |
| 비용 배수 | **2.11배** (N=3) | **1.76배** | **1.99배** | **1.61배** |

라운드트립 평균 1.77~2.18, 최대 3. 분포는 `air 1회:6·2회:10·3회:7` ·
`marryday 1회:5·2회:8·3회:9` · `apns4j 1회:10·2회:7·3회:5`.
도구별 호출수는 세 세트 합계로 `search_code 83 · grep 86 · read_file 52` —
**grep 이 가장 많이 불렸다.**

### 캡 분석 — 병목의 증거는 없다

| 세트 | 라운드트립 상한 걸림 | 안 걸림 | 절단 걸림 | 무절단 |
|---|---|---|---|---|
| air | 7/7 (1.00) | 10/10 (1.00) | 15/15 (1.00) | 2/2 (1.00) |
| marryday | 7/9 (0.78) | 5/7 (0.71) | 12/16 (0.75) | 0건 |
| apns4j | 5/5 (1.00) | 10/11 (0.91) | 12/13 (0.92) | 3/3 (1.00) |

라운드트립별 인용 정확도(합산)는 `1회 12/12 · 2회 13/16 · 3회 19/21`.
**캡에 걸린 쪽이 더 낮지 않다** — 오히려 같거나 높다. 표본이 작아 단정할 수 없지만,
`MAX_TOOL_ROUND_TRIPS = 3` 이나 `MAX_TOOL_RESULT_TOKENS = 800` 이 품질의 병목이라는
증거는 이 측정에 없다.

### 거절 자가 **두 번 연속 같은 방식으로** 샜다

측정 시점 거절 정확도는 도구 팔이 0.1667~0.5 였다. **답변 15건을 전부 읽었다 —
날조 0건이고, 15건 모두 없다고 정확히 답했다.** 0단계와 똑같다.

새는 구조도 똑같았다. **자가 화이트리스트라 답변 양식이 조금만 달라지면 샌다.**

| 샌 이유 | 건수 | 실제 표현 |
|---|---|---|
| 부정 술어가 목록에 없음 | 10 | `발견되지 않` · `검색되지 않` · `없고` · `없으며` |
| 스코프 어순·표현 | 3 | `소스 전체`(패턴은 `전체 소스`) · `저장소 전체` · `검색 범위` · `grep 결과` |

이번에는 **낱말을 더하지 않고 구조로 묶었다.** 부정 술어는 셋뿐이다 —
`없-`+어미(연결형 포함) · `[탐색 동사]+지 않` · 가능성 부정. 스코프는 범위 명사 하나를
**양쪽 어순으로** 감싼다(`전체 X` / `X 전체`). 재채점(89+67=156건, 무과금 $0)에서
12/15 가 잡히고 **정답 인용과의 충돌은 0건**이다.

**이것은 이번 실수가 아니라 이 자의 구조적 한계다.** 문구 매칭인 이상 다음에도 샌다 —
자를 고칠 때마다 기준선을 다시 읽어야 하고, 그게 무과금인 것이 이 설계의 값이다.

### 남은 3건은 자가 맞다 — 요구를 바꿨다

`md_ab_06/rag`(첫 문장이 답변, 거절은 마지막 문단) · `ap_ab_03/tool`(첫 문장이 "…에
대한 것이지 …아닙니다" — 동일성 부정이지 부재가 아니다) · `ap_ab_06/tool`(양보).

셋을 잡으려면 첫 문장 제한이나 양보 규칙을 풀어야 하고, 그 순간 "지어낸 답 + 면피
한 줄"이 거절로 통과한다. **자를 틀리게 만들어야 만족되는 요구는 요구가 틀린 것이다** —
0단계에서 같은 이유로 3번 요구를 바꿨다. `NOT_A_REFUSAL_OPENING` 목록으로 면제하되
**새로 새는 건이 생기면 여전히 깨지게** 두고, 면제가 낡으면(이제는 잡히면) 그것도
깨지는 테스트를 함께 뒀다.

### 내가 넣은 계열은 한 건도 안 걸렸다

`(?:읽어|열어|확인해|찾아|살펴)\s*본` 을 "도구 경로는 자기 행동으로 스코프를 말할
것"이라 보고 미리 넣었는데, **67건에서 한 건도 안 걸렸다.** 모델은 도구를 써도
스코프를 여전히 `검색된 범위`·`검색 결과`로 말한다. 예상이 빗나갔다.
지우지 않고 두는 것은 답변 양식이 바뀔 여지 때문이지 근거가 있어서가 아니다.

### 기준선 팔도 움직였다 — 노이즈와 구분되지 않는다

프롬프트를 고쳤으므로 `rag` 를 같은 실행에서 다시 쟀다. 인용 정확도가
`air 0.7059 → 0.7647` · `marryday 0.6875 → 0.6875` · `apns4j 0.75 → 0.8125` 이다.
**질의가 세트당 16~17개라 한 건이 0.06 이다** — air·apns4j 의 +0.0588/+0.0625 는
정확히 한 건 차이이고, **노이즈와 구분되지 않는다.** 프롬프트 효과로 읽지 말 것.
