# RepoDive 동작 플로우

GitHub 주소를 입력한 순간부터 대화가 끝날 때까지의 흐름.
설계 원칙은 둘이다 — **싼 검사를 먼저 하고 비싼 호출을 마지막에 한다.**
그리고 **오래 걸리는 일은 사용자를 기다리게 하지 않는다.**

---

## 한눈에 보기

```
[사용자] https://github.com/owner/repo 입력
    │
    ├─ POST /analyze ────────────────────────────────────
    │   URL 파싱 → 접근 확인 → 캐시 조회 → (미스면) 수집 → 요약
    │   응답 직전: 코드 색인을 **백그라운드 큐에 넣고** 바로 응답한다
    │   결과: 요약 마크다운 + session_id
    │
    │        ┌─ [백그라운드 워커] ─────────────────────────
    │        │   수집(tarball) → 청킹 → 임베딩 → pgvector 저장
    │        │   ※ 여기가 오래 걸린다 (청크당 약 0.16초)
    │        │   ※ 작은 저장소는 청킹·임베딩 없이 소스를 통째로 저장
    │        └────────────────────────────────────────────
    │
    └─ POST /chat  (질문) ────────────────────────────────
        색인 끝났으면 → 검색 → 답변
        아직이면    → 검색 없이 요약만으로 답변 (막지 않는다)
```

**색인은 첫 질문이 아니라 `/analyze` 끝에서 시작한다.** 첫 질문에서 동기로 돌리면
그 질문이 색인이 끝날 때까지 멈추는데, 큰 저장소는 10분이 넘어 기다릴 수 있는 시간이 아니다.

---

## 1단계 — URL 입력에서 요약까지 (`POST /analyze`)

### 순서

| # | 동작 | 코드 | 비용 |
|---|---|---|---|
| 1 | **URL 파싱** — owner/repo 를 소문자 키로 | `parse_github_url()` | 0 |
| 2 | **접근 확인** — 비공개·삭제·빈 저장소 걸러내기 | `check_repo_access()` | GitHub 1회 |
| 3 | **요약 캐시 조회** | `summary_cache.get(access)` | DB 1회 |
| 4 | ↳ **히트면**: 색인 시작 시도 후 세션 만들고 응답 | `_start_indexing()` | **LLM 0회** |
| 5 | 미스: **남용 제한 검사·예약** | `rate_limit.check_and_reserve()` | 0 |
| 6 | **컨텍스트 수집** — README·매니페스트·파일 목록 | `fetch_repo_context()` | GitHub 3+N회 |
| 6.5 | **소스 수집 + 정적분석** — 린터 집계 | `_analyze_sources()` | GitHub 1회 (tarball) |
| 7 | **프롬프트 조립** | `build_context(ctx, analysis)` | 0 |
| 8 | **요약 생성** | `run_summary()` | **LLM 1회** |
| 9 | **실행 기록** — 토큰·비용·시간 | `run_log.append_run()` | 0 |
| 10 | **스냅샷 저장** — 요약 + 컨텍스트 원문 | `summary_cache.put()` | DB |
| 11 | **코드 색인 시작** — 6.5에서 받은 소스와 함께 큐에 넣고 기다리지 않는다 | `indexer.start(files=...)` | 0 (여기서는) |
| 12 | **대화 세션 생성** | `_start_session()` | DB |

### 왜 이 순서인가

- **접근 확인(2)이 먼저인 이유**: 비공개·없는 저장소에 LLM 토큰을 쓰지 않기 위해.
  `GET /repos` 한 번으로 판정하고, 그 응답을 6단계에서 재사용해 중복 호출을 없앤다.
- **캐시 조회(3)가 제한 검사(5)보다 앞인 이유**: 캐시 히트에는 남용 제한을 걸지 않는다.
  LLM 비용이 0인 요청까지 한도를 깎을 이유가 없다.
- **제한 검사(5)가 수집(6)보다 앞인 이유**: 한도를 넘은 요청에 GitHub 요청 3~6회를 더 쓰지 않는다.
- **정적분석(6.5)이 요약(8)보다 앞인 이유**: 요약이 "이 코드베이스의 상태"를 말하려면
  요약을 만들기 전에 결과가 있어야 한다. 린터 자체는 언어를 다 합쳐 **약 3초**이고
  그중 대부분이 PMD 의 JVM 기동이다.
- **여기서 받은 소스를 색인으로 넘긴다(11단계).** 버리면 새 스냅샷마다 tarball 을 두 번
  받게 되는데, 아카이브 다운로드는 core 와 **별개 제한**을 받아 공짜가 아니다.
  넘기면 색인이 보는 소스가 요약·정적분석과 **같은 시점**이 되는 부수 효과도 있다 —
  스냅샷은 `pushed_at` 으로 고정돼 있으므로 같은 시점을 보는 쪽이 오히려 맞다.
  단 **소스가 `MAX_HANDOFF_CHARS`(20MB)를 넘으면 넘기지 않는다** — 수집 상한이
  파일 3,000개 × 200KB 라 이론상 600MB 까지 가능한데, tarball 한 번을 아끼자고
  워커가 처리할 때까지 그만한 메모리를 붙들 이유는 없다. 그때는 색인이 스스로 받는다.

### 정적분석 (6.5단계)

**설정도 의존성도 읽지 않는 린터만 쓴다.** 임의의 저장소를 받는 서비스라 ESLint 처럼
설정 파일(=JS 코드)을 실행하거나 `npm install` 이 필요한 도구는 **남의 코드를 우리 서버에서
실행하는 경로**가 된다.

| 언어 | 도구 | 규칙 | 설치 | 걸리는 시간 |
|---|---|---|---|---|
| Python | `ruff --isolated` | `E9,F` | `pip install -r requirements.txt` | 0.03초 |
| JS/TS | `oxlint --config <우리 것>` | `correctness` | `npm install` | 0.4초 |
| Java | `pmd check -R <우리 룰셋>` | `errorprone` (의견성 제외) | `python scripts/install_pmd.py` | 2.4초 (JVM 기동) |
| CSS | `stylelint --config <우리 것>` | correctness 상당 (관례 제외) | `npm install` | 1.1초 |

**하나도 없어도 서비스는 돈다** — 못 찾은 언어만 건너뛴다.

**HTML 은 일부러 안 붙였다.** 신호가 있는 도구(html-validate)가 우리가 `--config` 로
규칙을 못박아도 저장소의 `.htmlvalidate.js`·`.cjs` 를 **실행했고**, 검사도 침묵당했다
(2건 → 0건). ESLint·Pylint 를 뺀 것과 같은 이유다. htmlhint 는 안전성 이전에 21개 파일에
신호가 5건뿐이었다. `.scss` 도 검사하지 않는다 — stylelint 기본 파서가 읽지 못한다.

**함께 돌린다**(subprocess 대기라 스레드로 충분하다). 순차 2.77초 → 병렬 2.27초로,
PMD 의 JVM 기동이 병렬 시간을 지배한다. **결과 순서는 언어 순서로 고정한다** —
끝난 순서대로 담으면 같은 저장소도 실행마다 바이트가 달라져 캐시 접두사가 매번 깨진다.

**규칙을 고정하는 이유**: 기본값에 맡기면 도구 버전이 오를 때 요약 내용이 조용히 바뀐다.
같은 기준으로 **의견성 규칙을 뺀다** — 현대화 제안(ruff `UP*`, PMD `ReplaceJavaUtilDate`),
이름·중복 지적(PMD `AvoidDuplicateLiterals`), 관례(stylelint `no-descending-specificity` 는
CSS 39개 파일 192건 중 **155건**을 혼자 차지했다), 프레임워크 오탐(ruff `B008` 은 FastAPI 의
정석을 결함으로 본다). **오탐을 "결함"이라 요약에 적으면 조용히 틀린 말을 하게 된다.**

**빌드가 필요한 도구는 쓸 수 없다.** SpotBugs 류는 바이트코드를 보므로 저장소를 빌드해야 하는데,
남의 저장소를 빌드하는 것이 곧 임의 코드 실행이다.

**저장소가 검사를 끄지 못하게 두 겹으로 막는다.** oxlint 는 대상 트리의 `.oxlintrc.json` 을
자동으로 집어 가는데, 저장소가 그걸 심어 두면 **122건이 0건이 된다**(실측). 임시 디렉터리에
설정 파일을 아예 쓰지 않고, `--config` 로 우리 규칙을 못박는다.
(JS 설정 파일은 심어도 실행되지 않는 것을 부작용 관찰로 확인했다. stylelint 도 같은 관찰을
했고 — `--config` 를 주면 저장소 설정을 읽지도 실행하지도 않는다. 다만 `--config` 없이 두면
저장소 설정이 검사를 통째로 끄므로 그 인자가 곧 방어다.)

프롬프트에는 **집계만** 넣는다(건수·상위 규칙·경고가 몰린 파일). 실측 189~938토큰이고,
스냅샷마다 고정이라 캐시 접두사에 들어간다. **검사한 언어와 규칙을 함께 적는다** —
안 적으면 검사 대상 언어가 없어 못 잰 저장소와 깨끗한 저장소를 모델이 구분하지 못한다.
잰 것이 없으면 섹션 자체가 빠지고, 요약 프롬프트도 **블록이 있을 때만** `## 코드 상태` 를
쓰도록 지시한다("린터를 안 돌린 것"과 "코드가 깨끗한 것"은 다르다).
- **캐시 히트에도 색인을 시작하는 이유(4)**: 요약만 재사용할 뿐, 그 스냅샷은 아직
  색인 전일 수 있다. 색인은 첫 분석 뒤에도 한참 걸린다.

### 캐시 키

```
{정식표기 소문자}@pushed_at:{타임스탬프}
예) react/react@pushed_at:20260813154443
```

- **입력한 URL 이 아니라 GitHub 이 알려준 정식 표기**를 쓴다 → 저장소가 이전돼도 같은 캐시로 모인다
- 무효화 기준은 커밋 SHA 가 아니라 **`pushed_at`** — `GET /repos` 응답에 이미 있어 추가 요청이 0회다

### 응답

```json
{ "repo": {...}, "summary": "## 프로젝트 개요\n...", "session_id": "uuid 또는 null" }
```

`session_id` 가 `null` 이면 DB 를 쓸 수 없는 상태다. 프론트는 대화 UI 를 숨기고 요약만 보여준다.

---

## 2단계 — 코드 색인 (백그라운드)

요청 스레드가 아니라 **큐 워커 하나**가 순서대로 처리한다. 사용자는 기다리지 않는다.

### 큐를 거치는 이유

임베딩은 CPU 를 다 쓴다. 첫 색인과 관리자 재색인이 서로 모른 채 동시에 돌면 둘 다 느려지고,
큰 저장소 서너 개가 겹치면 어느 것도 끝나지 않는다. 경로를 하나로 모아 직렬화한다.

- 중복 판정은 **DB 가 한 문장으로** 한다 (`index_status.begin()` — 같은 대상에
  pending/running 이 있으면 `None`). 큐를 보고 판정하면 이미 꺼내 처리 중인 작업을 놓친다
- 스냅샷별 잠금이 그 판정과 실행 사이의 틈을 한 번 더 닫는다
- **이미 색인이 있으면 아무것도 하지 않는다.** 규칙(`rule_version`)이 낡았어도 자동 재색인은
  하지 않는다 — 배포 직후 전 저장소가 한꺼번에 임베딩을 시작하면 그게 곧 장애다.
  재색인은 관리자가 트리거한다(`/admin/api/rebuild-index`)
- **메모리 큐라 단일 프로세스 전제다** (`uvicorn --workers 2` 이상이면 워커마다 다른 큐)

### 빌드 내용 (`indexer.run_build`)

```
소스 확보             /analyze 가 넘겨준 것이 있으면 그대로 쓰고,
                     없으면(캐시 히트·정적분석 실패·너무 큼) tarball 1회로 받는다
       ▼
 ┌── 작은 저장소인가? (_try_full_injection) ──┐
 │ 예 → 소스를 통째로 스냅샷에 저장하고 끝     │  청크 0개로 '완료'
 │      (repo_snapshots.source_bundle)        │  임베딩 없음
 └────────────────────────────────────────────┘
       │ 아니오
       ▼
chunk_files()          tree-sitter 로 메서드 단위 분할 (토큰 한도 넘으면 재분할)
       ▼
embed_documents()      로컬 ONNX 임베딩 (무과금) ← 시간의 대부분
       ▼
insert_chunks() → index_status.complete()   완료 표시와 활성 포인터 교체가 한 트랜잭션
```

- **전체 주입 판정은 크기 속성으로만 한다** (CLAUDE.md §7). 바이트 합으로 명백히 큰 저장소를
  먼저 걸러내고(`FULL_INJECTION_MAX_SOURCE_BYTES`), **통과 판정은 실제 토큰 수로** 한다
  (`FULL_INJECTION_MAX_TOKENS`). 바이트 추정이 저장소에 따라 24~32% 어긋났기 때문이다
- 진행 상태는 DB(`index_builds`)에 남긴다 — 화면이 "N/M 개"를 보여줄 근거이자 실패 사유의 전달 경로
- **실패해도 활성 포인터는 그대로다.** 재색인이 실패해도 쓰던 색인은 살아 있다
- 소스가 없는 저장소도 청크 0개로 '완료'로 남긴다 — 안 그러면 질문할 때마다 tarball 을 다시 받는다

---

## 3단계 — 질문 (`POST /chat`)

### 순서

| # | 동작 | 코드 | 비용 |
|---|---|---|---|
| 1 | 질문 검증 (빈 문자열 거부) | | 0 |
| 2 | **세션 조회** — 스냅샷·컨텍스트·요약·소스 번들을 함께 | `chats.get_session()` | DB 1회 |
| 3 | **남용 제한** — 질문 하나 = LLM 하나 | `rate_limit.check_and_reserve()` | 0 |
| 4 | **대화 이력** — 최근 20개 | `chats.list_messages()` | DB 1회 |
| 5 | **근거 확보** ↓ 세 갈래 | | DB |
| 6 | **답변 생성** | `run_chat()` | **LLM 1회** |
| 7 | **질문·답변 저장** — 한 트랜잭션 | `chats.add_exchange()` | DB |
| 8 | 실행 기록 · 토큰 기록 | | 0 |

### 5단계의 세 갈래

| 상태 | 하는 일 |
|---|---|
| **소스 번들이 있다** (전체 주입 스냅샷) | 검색하지 않는다. 소스가 이미 전부 프롬프트에 있어 검색해 봐야 그 일부를 두 번 보내는 셈이고, 스니펫은 캐시 뒤라 매 질문 정가다 |
| **색인 완료** (`is_ready`) | 질문 임베딩 → 코사인 상위 8개 → 경로·행 범위를 헤더로 붙여 조립 |
| **아직 진행 중·시작 전** | 검색을 건너뛰고 요약만으로 답한다. 세션만 복원해 들어온 경우(`/analyze` 를 거치지 않음)엔 여기서 색인을 시작시킨다 |

**진행 중인 인덱스로는 검색하지 않는다.** 아직 임베딩하지 않은 코드가 '저장소에 없는 코드'처럼
보여 틀린 답을 만든다. 검색은 **활성 빌드 안에서만** 찾는다 — 재색인 중이면 새 빌드의
절반짜리 청크가 함께 들어 있다.

검색은 가져온 뒤 저정보 청크(스타일시트·선언만 있는 조각 등)에 `LOW_INFO_PENALTY` 를 더해
순서를 다시 세운다. **제외가 아니라 감점이다** — 스타일시트가 정답인 질문도 있다.

### 프롬프트 조립 (6단계)

```
system : CHAT_SYSTEM_PROMPT
user   : 스냅샷 원문(컨텍스트) [+ 소스 번들]   ← cache_control 지점
assistant: 요약
...최근 대화 20개...
user   : [검색된 코드 스니펫] + 질문            ← 여기에만 검색 결과가 붙는다
```

- **검색된 스니펫은 반드시 마지막 메시지에 붙인다.** 질문마다 내용이 달라지므로
  `cache_control` 이 걸린 블록에 넣으면 매 질문이 캐시를 깨뜨려 스냅샷 전체를 정가로 다시 계산한다
- **소스 번들은 반대로 캐시 지점에 넣는다.** 스냅샷마다 고정이라 캐시가 산다
- 이력을 20개로 자르는 지점은 캐시 지점보다 뒤라서 캐시 접두사는 깨지지 않는다
- 스니펫 본문에는 줄 번호를 붙이지 않는다(헤더의 행 범위만). 붙였다가 되돌렸다 —
  `_merge_small` 이 조각을 이어붙이면서 청크의 48.8% 가 원본 줄 수와 어긋나기 때문이다

---

## 프론트엔드 흐름

```
URL 입력 → POST /analyze
    │
    ├─ 진행 표시: 경과 시간 기반 3단계 (저장소 확인 → 파일 수집 → AI 요약 작성)
    │
    ├─ 응답의 session_id 수신
    │       │
    │       ├─ localStorage 에 이 저장소의 이전 session_id 가 있는가?
    │       │     ├─ 있음 → GET /chat/{id} 로 이력 복원 시도 (200이면 이어감)
    │       │     └─ 없음 → 이번에 만들어진 세션 사용 + localStorage 저장
    │       │
    │       └─ session_id 가 null → 대화 UI 를 통째로 숨김
    │
    ├─ 대화 화면 진입 → GET /chat/{id}/index 를 주기적으로 폴링
    │       └─ pending/running 이면 배너: "코드를 읽는 중입니다 (N/M 개 · 약 X분 남음).
    │          지금 물어보면 요약을 근거로만 답합니다"
    │          completed 면 배너 없음, failed 면 사유를 알린다
    │
    └─ 질문 입력 → POST /chat  (응답까지 경과 시간을 시계로 표시)
```

- **분석 요청에 저장된 session_id 를 함께 보낸다.** 서버는 localStorage 를 볼 수 없어서,
  알려주지 않으면 분석할 때마다 새 세션을 만들고 그 세션은 메시지 없이 버려진다
- localStorage 키는 `repodive:session:{owner}/{name}` (소문자).
  저장할 때는 **입력한 URL 이 아니라 응답의 정식 표기**로 만들어 백엔드 캐시 키와 기준을 맞춘다
  (분석 전 조회는 정식 표기를 모르므로 입력 URL 에서 추정한다 — 못 찾으면 전과 같이 동작할 뿐이다)
- 전송 중인 질문은 `messages` 가 아니라 `pending` 에 둔다. 백엔드가 LLM 성공 후에야
  질문·답변을 함께 저장하므로, 실패한 질문을 화면에 남기면 새로고침 때 사라져 화면과 DB 가 어긋난다
- 남은 시간은 **지금까지의 실제 속도로** 계산한다(`elapsed / done × 남은 개수`).
  청크당 시간은 저장소·기기마다 달라 고정값을 박으면 늘 틀린다

---

## 비용 요약

| 시나리오 | GitHub 요청 | LLM 호출 | 걸리는 시간 |
|---|---|---|---|
| 첫 분석 (캐시 미스) | **5~7회** (tarball 1회 포함) | 1회 | **11~55초** |
| ↳ 그 뒤 색인 | **0회** (넘겨받은 소스 재사용) | 0회 | 백그라운드 |
| 재분석 (캐시 히트) | **1회** | **0회** | 1초 미만 |
| 색인 (백그라운드) | 1회 (tarball) | **0회** | 청크당 약 0.16초 |
| 질문 | 0회 | 1회 | 수 초 |

색인 실측(int8 양자화 e5-large, 2026-08-19):

| 규모 | 시간 |
|---|---|
| 236청크 (작은 Java·JS 혼합) | 40초 |
| 4,365청크 (3D 에셋이 든 큰 저장소) | 10.6분 |
| 전체 주입 (0청크) | 1초 |

- 임베딩은 **로컬 ONNX 라 과금이 없다**. 오래 걸리는 것은 비용이 아니라 CPU 시간이다
- 대화의 실효 입력 배율은 캐시 덕에 약 0.45배 (읽기 0.1배·쓰기 1.25배)
- GitHub 미인증은 시간당 60회, 토큰이 있으면 5,000회

---

## 실패했을 때의 동작

각 단계가 실패해도 **가능한 한 서비스를 이어간다.** 부가 기능의 실패가 본 기능을 막지 않는 것이 원칙이다.

| 실패 지점 | 결과 |
|---|---|
| URL 형식 오류 | 400 |
| 대화 id 형식 오류 (`/chat`) | 400 (`/analyze` 는 무시하고 새로 만든다) |
| 비공개·없는 저장소 | 404 |
| 빈 저장소 | 400 |
| GitHub 한도 초과 | 429 (+ `Retry-After`) |
| 우리 쪽 남용 제한 | 429 (+ `Retry-After`) |
| Claude API 오류 | 502 |
| API 키 없음 | 503 |
| **DB 장애 (`/analyze`)** | 요약은 정상 응답. 캐시가 꺼지고 `session_id=null` → 대화 UI 만 숨겨짐 |
| **DB 장애 (`/chat`)** | 503. 대화는 DB 없이 대신할 방법이 없다 |
| **세션 생성 실패** | 요약은 그대로 응답 |
| **정적분석 실패** (소스 수집 실패·린터 없음·타임아웃) | 요약은 정상 진행. 정적분석 섹션만 빠진다 |
| **색인 시작 실패** | 삼킨다. 분석은 정상 응답 |
| **색인 빌드 실패** | 질문은 진행(요약만으로 답변). 화면에 사유 배너. 활성 포인터는 그대로라 옛 색인이 있으면 계속 쓴다 |
| **코드 검색 실패** | 빈 스니펫으로 진행 |
| **대화 저장 실패** | 답변은 돌려주고 기록만 포기 (토큰은 이미 썼으므로) |
| **실행 로그 쓰기 실패** | 경고만 남기고 진행 |
| **남용 제한 저장소 장애** | 막지 않고 통과시킨다. 상한은 비용을 지키는 장치이지 서비스의 관문이 아니다 |

---

## 데이터가 어디에 남는가

| 저장소 | 내용 |
|---|---|
| `repos` | 저장소 (소문자 키 + 표시용 정식 표기) |
| `repo_snapshots` | 특정 `pushed_at` 시점의 컨텍스트 원문(**정적분석 집계 포함**) + 요약. `source_bundle` 이 차 있으면 **전체 주입 스냅샷** |
| `chat_sessions` | 대화. **`snapshot_id` 로 보는 코드 버전이 고정된다** |
| `messages` | 질문·답변 |
| `index_builds` | 색인 빌드의 상태·진행·활성 포인터 |
| `CHUNK_TABLE` 이 가리키는 청크 테이블 | 코드 청크 + 임베딩 (HNSW 인덱스). 모델이 바뀌면 **테이블을 나눈다** — 차원이 같아도 섞이면 오류 없이 거리만 틀어진다 |
| `runs` | 호출별 토큰·비용·시간 → `/admin/runs`, `/admin/usage` |
| `rate_limit_daily` · `rate_limit_hits` | 남용 방지 카운터 (IP 는 합계가 아니라 개별 시각 — 윈도우가 슬라이딩이라) |
| `logs/search_evals.jsonl` | 검색 품질 측정 → `/admin/search` |

- **`runs` 와 남용 제한은 DB 가 있으면 DB, 없으면 파일로 폴백한다**
  (`logs/runs.jsonl`, `cache/rate_limit.json`). 다중 워커에서 파일 경로는 카운터가 새기 때문에
  DB 를 우선하고, DB 없는 환경도 그대로 돌게 남겨 뒀다
- **청크를 저장소가 아니라 스냅샷에 매다는 이유**: 대화 세션이 보는 코드 버전은
  `chat_sessions.snapshot_id` 로 확정돼 있다. 저장소가 갱신돼 새 스냅샷이 생겨도
  진행 중인 대화는 자기가 색인한 코드를 계속 본다
