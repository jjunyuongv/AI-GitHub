# TROUBLESHOOTING

RepoDive 를 만들며 **실제로 겪은** 문제와 해결을 한곳에 모은 색인이다.
증상이 낯익으면 여기서 먼저 찾고, 상세 경위는 `docs/log/` 의 해당 작업 절을 본다.

- `plan.md` — 작업 로그 색인. 본문은 `docs/log/` 6개 파일에 시간순으로 나뉘어 있다
- `docs/log/*.md` — 시간순 작업 로그. "언제 무엇을 왜 했나"
- **이 문서** — 증상별 색인. "이 증상을 전에 겪었나"
- `트러블슈팅.md` — 코드 검색 개선(2026-08-16~18)의 **서술형 상세 기록**. 그 시점에서 멈춰 있다

---

## 이 프로젝트에서 반복된 실패 방식

개별 버그보다 이 패턴이 더 값진 자산이다. 새 기능을 붙일 때 이것부터 의심할 것.

1. **조용히 틀린다.** 예외도 실패도 없이 품질·수치만 나빠진다 — 접두어 누락, 캐시 토큰 누락,
   테스트 격리 우회, 화면이 청크를 0개로 세기.
   → **오류가 안 나는 곳일수록 테스트로 고정한다.**
2. **의도한 것과 다른 것을 잰다.** 하네스가 대상이 아니라 자기 파서를 재거나(행 번호 정확도),
   청크 검색이 아니라 파일 검색을 재고 있었다(평가셋 v1).
3. **저장한 것과 읽는 것의 전제가 어긋난다.** 완료 판정, 규칙 버전, 표시 표기,
   타입(datetime vs str), 테이블 목록.
4. **주석으로 의도를 적어 둔 것만으로는 회귀를 막지 못한다.** `schema.sql` 과 `indexer.py` 에
   "청크 0개도 완료로 남긴다"고 써 뒀는데도 그대로 깨졌다. 의도는 **테스트로** 고정한다.
5. **여러 결함을 한 번에 고치지 않는다.** 청킹 2a→2b→2c 를 나눠 매 단계 평가한 덕에
   2a·2b 가 한국어 검색을 깎아먹는 것을 중간에 알아챘다. 한꺼번에 고쳤다면 원인을 못 찾았다.

---

## GitHub 수집

| 증상 | 원인 | 해결 |
|---|---|---|
| 조직 이전된 저장소가 301 | 리다이렉트 미추적 | `follow_redirects=True` |
| `pyproject.toml` 을 못 찾음 | 트리 200개로 자른 **뒤** 매니페스트를 탐색 | 자르기 **전** 전체 트리에서 탐색 |
| `?tab=`·`/tree/main` 붙으면 400 | 정규식 `...$` 앵커 | `urlsplit` 으로 재작성, 3번째 이후 세그먼트 버림 |
| 헤더가 `microsoft/typescript` 로 보임 | 정규화한 소문자 키를 화면에도 씀 | 키(API·로그)와 표시(`owner.login`)를 분리 |
| 소스 수집이 rate limit 을 태움 | 파일마다 `/contents` 호출 (47파일 → 48회) | **tarball 1회** (2회) |
| 큰 저장소 수집이 413 | `MAX_ARCHIVE_BYTES` 80MB | 500MB + `SpooledTemporaryFile`(32MB 초과분 디스크) |
| **`/rate_limit` 은 여유인데 tarball 이 429** | 아카이브 다운로드는 core(5,000/h)와 **별개 제한**을 받는다 | 같은 저장소를 반복해 받는 실험은 **로컬 캐시를 먼저** 만든다 (`cache/eval_sources/`) |

## 임베딩 · 모델

| 증상 | 원인 | 해결 |
|---|---|---|
| 한국어 질의가 안 맞음 | jina-code 의 "Multilingual" 은 **프로그래밍 언어 30종**, 자연어는 영어 전용 | e5-large 로 교체 (한국어 R@8 0.17 → 0.67) |
| 접두어가 조용히 빠짐 | fastembed `query_embed()` 는 접두어를 **안 붙인다**(`embed()` 를 그대로 부름) | 직접 부착 + 테스트 고정 |
| `query:질문` 처럼 붙어버림 | `.env` 파서가 끝 공백을 자름 | **따옴표 필수** (`"query: "`) |
| 1024 벡터가 안 들어감 | `vector(N)` 은 차원 고정 | **차원별 테이블** (`code_chunks_1024` 등) |
| 잘린 청크가 늘 0 | 토크나이저가 이미 512 로 **잘라서** 돌려줌 | 초과(>)가 아니라 **도달(>=)** 로 센다 |
| 축소해도 한도 안에 안 들어옴 | `count_tokens` 가 잘린 값(512)만 보고 → 얼마나 넘었는지 모름 | `no_truncation()` 을 **사본에만** 걸어 실제 수를 센다 |
| 피크 메모리가 2.1GB 모델인데 68MB | `tracemalloc` 은 파이썬 힙만 봄 (ONNX 네이티브 제외) | 그 수치를 믿지 말 것 — RSS 로 재야 한다 |
| 모델을 매번 재다운로드 | 캐시 기본값이 OS 임시 폴더 | `Back/cache/models` 로 고정 |
| 임베딩이 느림 | 배치가 크면 짧은 청크까지 최장 청크에 맞춰 패딩 | 실측으로 결정. **모델이 바뀌면 최적값도 바뀐다** (jina-fp32 8 → e5-fp32 32 → **e5-int8 1**) |
| 배치를 키워도 안 빨라짐 | onnxruntime 이 청크 하나짜리 입력에도 **이미 전 코어를 쓴다** → 배치로 얻을 병렬성이 없고 패딩 낭비만 남는다 | 코어가 넉넉할 때만 그렇다. 아래 줄과 함께 볼 것 |
| 200청크 벤치로 상수를 바꿨다가 사고 | 표본이 실제 작업(4,000청크)의 조건을 재현하지 못한다 | 벤치로 후보를 좁히고 **실제 재색인으로 확인**한 뒤 바꾼다 |
| **같은 배치인데 회차마다 1.8~2배 차이** | CPU 점유 7.7x → 6.6x (14% 차이)가 시간 **1.84배**를 만든다. 점유율은 "P코어를 쓰는가"의 대리 지표라 선형이 아니다 — 예산이 줄어 LP E코어로 몰리면 점유는 조금 떨어지는데 처리량은 무너진다 | 실측에 **CPU 점유를 함께** 남기고, 점유가 다르면 **시간을 견주지 말 것** |
| 배치 크기를 실측으로 못 정하겠다 | 노트북에서는 통제 못 하는 변수(CPU 상태)의 영향이 재려는 효과(배치)보다 크다. 실제로 결론이 **세 번 뒤집혔다** | **배포 기기**에서 재거나 CPU 친화도·전원 계획을 고정한다. 못 하면 기존 값을 지킨다 |
| 배치를 바꿨더니 검색 결과가 미묘하게 달라짐 | **배치는 벡터를 바꾼다.** 배치 32 ↔ 1 로 만든 같은 청크의 코사인이 평균 0.993 · 최소 0.977 (int8 ↔ fp32 차이와 같은 크기) | 배치 변경은 **재색인 + 품질 재측정**을 동반한다. "패딩은 마스크로 가려지니 같다"는 틀렸다 |
| 지난 실측을 지금 것과 못 견줌 | 시간만 적고 **조건(CPU 점유)을 안 적었다** → 그 회차가 정상이었는지 알 길이 없다 | 실측에는 **시간과 조건을 함께** 남긴다. 42.6분짜리 측정 하나가 이것 때문에 통째로 버려졌다 |
| 벤치마크 회차가 통째로 느려짐 | 7분짜리를 연달아 돌려 기기가 지친다(모바일 CPU 지속 부하) | 회차 사이에 **기기를 쉬게 한다**(10분). 배율은 인터리브가 지키지만 절대값은 못 지킨다 |
| 멀쩡한 회차를 카나리가 폐기 | 카나리 표본이 짧아 몇 초가 안 걸림 → 0.2초 노이즈가 14% 로 잡힌다 | 카나리는 **가장 민감한 조건으로, 몇 초는 걸리게** 잰다 (둘 다 필요하다) |
| 같은 코드인데 벡터가 달라질 위험 | fastembed 가 pooling 을 바꾼 전례(CLS → mean) | 버전 상한 `<0.9` + 범위 이탈 테스트 |
| 목록에 없는 모델을 쓰고 싶다 | fastembed 내장 목록에 다국어 검색 모델이 e5-large 하나뿐 | `add_custom_model()`. 이름을 **다르게** 주어야 규칙 해시가 갈린다 |

## 정적분석

| 증상 | 원인 | 해결 |
|---|---|---|
| **저장소가 검사를 통째로 끌 수 있다** | oxlint 가 대상 트리의 `.oxlintrc.json` 을 자동으로 집어 간다 — 심어 두면 **122건 → 0건** | 두 겹: 임시 디렉터리에 **설정 파일을 안 쓴다** + `--config` 로 우리 규칙을 못박는다 |
| 린터가 남의 코드를 실행할까 | ESLint 는 `eslint.config.js`(=JS 코드)를 실행하고 `npm install`(=`postinstall`)이 필요하다. Pylint 는 추론 중 import 한다 | **설정·의존성을 안 읽는 린터만** 쓴다(ruff, oxlint). oxlint 의 JS 설정 실행 여부는 부작용을 심어 확인했다 — 실행 안 함 |
| 기본 규칙이 스타일 잡음뿐 | 도구 기본값은 현대화 제안까지 켠다(ruff 기본 1,125건 중 대부분) | 규칙을 **명시적으로 고정**한다(`E9,F` / `correctness`). 안 그러면 도구 버전이 오를 때 요약이 조용히 바뀐다 |
| `B008` 90건이 전부 오탐 | FastAPI 는 `Depends()`·`File()` 을 인자 기본값에 두는 것이 정석인데 bugbear 가 결함으로 본다 | 그 규칙군을 빼거나 프레임워크 예외를 설정한다. **오탐을 "결함"이라 요약에 적으면 조용히 틀린다** |
| 빌드가 필요한 도구를 쓰고 싶다 | SpotBugs 등은 **바이트코드**를 본다 → 저장소를 빌드해야 한다 | 쓸 수 없다. 남의 저장소를 빌드하는 것이 곧 임의 코드 실행이다 |
| 린터를 붙였더니 멀쩡한 테스트가 깨짐 | "분석 대상이 아닌 언어"의 픽스처로 **Java** 를 쓰고 있었는데 Java 가 대상이 됐다 | 옳게 깨진 것이다. 픽스처를 지금도 미검사인 언어로 바꾼다 (CSS 도 이후 대상이 되어 두 번 바뀌었다 → 지금은 HTML·SCSS) |
| **stylelint 가 어떤 저장소든 0건** | 경고를 찾으면 결과 JSON 을 **stderr 로** 낸다(그때 stdout 은 빈다). 다른 린터와 반대다 | `stdout or stderr` 순으로 읽는다. 실측에서 39개 파일 192건이 0건으로 보였다 |
| CSS 경고의 81%가 한 규칙 | `no-descending-specificity` 는 특이도 **순서 관례**지 결함이 아니다 (192건 중 155건) | 뺀다. 남은 37건은 shorthand 덮어쓰기·중복 속성·중복 셀렉터로 실수 쪽이었다 |
| **HTML 린터는 붙이지 못한다** | html-validate 는 우리가 `--config` 로 규칙을 못박아도 저장소의 `.htmlvalidate.js`·`.cjs` 를 **실행**했고 검사도 침묵당했다(2건 → 0건) | ESLint·Pylint 와 같은 이유로 탈락. htmlhint 는 안전성 이전에 21개 파일에 신호가 5건뿐 |
| Java 분석만 유독 느림 | JVM 기동이 2.4초다(ruff 0.03초·oxlint 0.4초) | 정상이다. 테스트에서 PMD 실행은 **한 건만** 둔다 |
| 같은 tarball 을 두 번 받는다 | 정적분석(`/analyze`)과 색인이 각각 받았다 | `/analyze` 가 받은 소스를 큐로 넘긴다. 단 **크기 상한**을 둔다 — 수집 상한이 3,000파일 × 200KB 라 이론상 600MB 를 큐에 붙들 수 있다 |
| 병렬로 돌렸더니 캐시가 매번 깨짐(가능성) | 결과를 **끝난 순서**대로 담으면 같은 저장소도 실행마다 바이트가 달라진다. 이 집계는 `cache_control` 이 걸린 접두사에 들어간다 | 제출 순서(=언어 순서)로 거둔다. `build_source_bundle` 이 경로 순으로 정렬하는 것과 같은 이유 |

## 청킹 · 검색

| 증상 | 원인 | 해결 |
|---|---|---|
| 컨트롤러 클래스가 질문과 무관하게 늘 상위 | 클래스를 통째로 두면 벡터가 "그 클래스가 하는 모든 일"의 **평균** | 컨테이너는 크기와 무관하게 **메서드로 내려간다** |
| 청크의 38%가 import | import 노드를 그대로 청크로 | 제외 (중앙값 402자 → 1068자) |
| **정답이 순위가 아니라 인덱스에 부재** | 클래스 헤더가 `MIN_CHUNK_CHARS` 미달로 통째 폐기 | 짧아도 버리지 않는다. **검색을 아무리 고쳐도 없는 건 못 찾는다** |
| 헤더를 살리니 메서드 둘이 한 청크로 | `_merge_small` 의 연쇄 병합 | "작은 조각은 이웃 **하나**에만 붙는다" |
| CSS 가 한국어 질의 상위를 차지 | 한국어 주석이 한국어와 잘 맞음. 게다가 잘게 쪼개지며 **온전히 임베딩돼 더 잘 맞게 된** 역설 | 저정보 청크 **감점**(제외 아님), **검색 단계**에서 — 저장 단계면 규칙 바꿀 때마다 재색인 |
| Python/JS 에서 청크 11개 잘림 | "정의가 아닌 최상위 요소"를 **크기 검사 없이** 통째로 넣음 | 그 분기도 `_line_chunks` 로 내려보냄 |
| 인덱싱이 22분 → 96분으로 늘어남 | 청크 수 때문이 아니다. **잘림이 없어진 것**이 이유 — 전에는 512 에서 잘려 뒷부분을 계산하지 않았다 | 잘림 0 을 유지하는 한 그 시간은 돌아오지 않는다. 단축은 모델 쪽에서 |
| 문자 상한 → 토큰 상한 전환 | air 는 개선, marryday 는 한국어 R@8 0.80 → 0.60 붕괴 | **800자 복귀.** 토큰 재분할·토크나이저 수정만 유지 |
| 스니펫에 줄 번호를 붙였더니 틀림 | 청크 content 가 원본 줄과 1:1 이 아니다 — 병합이 사이 줄을 빠뜨려 **48.8% 가 줄 수부터 어긋난다** | 붙이지 않는다. 모델이 세는 편이 낫다(틀린 번호는 그대로 믿는다) |

## 인덱싱 생애

| 증상 | 원인 | 해결 |
|---|---|---|
| 재색인이 **5초 만에 완료**로 보임 | `index_status.get()` 은 **활성 빌드를 우선** 돌려준다(재색인 중에도 화면에 '읽는 중' 배너를 안 띄우려고) → 직전 완료 빌드를 본 것 | 진행 조회는 **`build_id` 로** 한다. `get()` 은 사용자 화면용이다 |
| 빌드가 `running` 인 채 재시도가 막힘 | 프로세스가 죽으면 워커 스레드도 사라진다(`begin()` 은 pending/running 이 있으면 None) | 기동 시 `reset_running()` 이 정리한다. 수동이면 `fail()` 로 사유를 적어 닫는다 |

| 증상 | 원인 | 해결 |
|---|---|---|
| 질문마다 80MB 를 다시 받음 | `is_indexed()` 가 **청크 행 존재**로 완료를 판단 → 청크 0개면 영원히 미완료 | 완료 판정을 `snapshot_index_status` 로 이전. **청크 0개도 완료는 완료** |
| 청킹을 고쳐도 사용자에게 안 닿음 | 재색인은 새 스냅샷이 생길 때만. **어떤 규칙으로 만든 인덱스인지 알 방법이 없었다** | `rule_version()` (자동 해시) + `/admin/snapshots` |
| 재색인 중 챗봇이 코드 없이 답함 | DELETE 후 INSERT 라 40~96분간 청크가 없음 | 새 빌드를 따로 쌓고 완료 시 **포인터만 교체** (롤백도 포인터로) |
| 기록된 청크 수와 실제가 다름 | 평가 하네스가 `replace_chunks()` 만 부르고 `complete()` 생략 | 하네스도 정식 경로로. 화면은 `chunks_actual`(실측) 표시 |
| **새 색인이 화면에 "청크 0개"** | `list_all()` 의 CTE 와 `cleanup.ALL_CHUNK_TABLES` 에 **테이블 이름 하드코딩** — 테이블을 늘리며 둘 다 놓침 | 목록을 `index_status.CHUNK_TABLES` 한 곳으로 + `schema.sql` 대조 테스트 |
| 재색인 중 "코드를 처음 읽는 중" 배너 | `index_status.get()` 이 진행 중 빌드를 반환 | **활성 빌드 우선** 반환 |
| 이미 인덱싱된 저장소에도 배너 | 프론트가 **8초 경과**로 추정 | 실제 상태 폴링(`GET /chat/{id}/index`) |
| ETA 가 29분짜리를 58분으로 | 기준 시각이 **수집 시작** → tarball 15~20초가 청크당 속도에 섞임 | 임베딩 시작 시각을 다시 찍는다 |
| 재시도가 영원히 막힘 | 인덱싱 스레드는 프로세스와 함께 사라지는데 `running` 행이 남는다 | 기동 시 `reset_running()` |
| 동시 요청 둘이 함께 인덱싱 시작 | "아직 안 했네" 조회와 표시가 나뉘어 있음 | `INSERT … ON CONFLICT … WHERE` **한 문장**으로 판정+표시 |

## DB · 저장소

| 증상 | 원인 | 해결 |
|---|---|---|
| **대화 UI 가 말없이 사라짐** | Docker 미기동 → `session_id=None` → 프론트가 대화를 숨김. 요약은 정상이라 원인을 짚기 어렵다 | **웹 열기 전에 `cd Back; docker compose up -d`.** 진단: `GET /chat/{아무 uuid}` → 404 면 정상, 503 이면 DB 끊김 |
| `UndefinedFunction` (`<=>`) | 파이썬 list 가 `float8[]` 로 감 | **`%s::vector` 캐스트 필수** (어댑터는 결과 파싱 담당) |
| `code_chunks` 생성 실패 | pgvector 확장이 공식 `postgres:17` 이미지에 없다 | `pgvector/pgvector:pg17` 사용 |
| 요청이 130초 매달림 | 접속 타임아웃 기본값이 무제한 | `connect_timeout` + 풀 `timeout` 5초 |
| 종료 시 `PythonFinalizationError` | 풀을 열어 둔 채 인터프리터 종료 | `atexit.register(close)` |
| 다음 질문에서 API 가 호출 거부 | 질문을 먼저 저장 → 실패 시 이력이 `user` 로 끝나 역할이 안 번갈아감 | 질문·답변을 **LLM 성공 후 한 트랜잭션**(`add_exchange`) |
| UUID 아닌 id 가 503 으로 뭉개짐 | DB 가 형식 오류를 냄 | 형식 검증을 **먼저** (400) |
| **다중 워커에서 상한이 거의 안 걸림** | 파일 카운터를 프로세스 Lock 으로만 보호 → 동시 쓰기로 JSON 이 깨지고 카운터가 리셋 | DB(SQL 원자성) 우선 + 파일 폴백. 실측 **3/50 → 50/50** |
| 빈 `chat_sessions` 누적 | `/analyze` 가 복원 여부를 모른 채 매번 세션 생성 | **클라이언트가 알려준다** (`AnalyzeRequest.session_id`) |

## 비용 · 측정

| 증상 | 원인 | 해결 |
|---|---|---|
| 비용이 32.3% 과소 집계 | `estimate_cost` 가 `input_tokens` 만 셈 — **캐시 쓰기/읽기 누락** | 캐시 토큰을 단가에 반영. 일일 상한도 같은 누락이었다 |
| 단가 만료를 사람이 잊으면 조용히 33% 적게 | 도입가가 상수로 박힘 | `pricing_for(model, at)` — 날짜로 갈린다 |
| 임계값이 3배 헐거워질 뻔 | 캐시 "1/10" 은 **읽기 단가**일 뿐. 쓰기 1.25배를 세션 길이로 나눠야 함 | 실효 배율 `w̄ = Σ(1.25+0.1(N−1))/ΣN` = 0.4538 (0.33 은 27% 낙관) |
| **임베딩 시간이 저장소마다 정반대** | A 전부 → B 전부 순서라 **배경 부하가 한쪽에만 실림** | **인터리브 A→B→A→B** + 중앙값 + 카나리 |
| 배치 재측정이 앞선 결과와 반대 | 96분 연속 CPU 100% 뒤라 **열 스로틀링**이 배치 효과보다 크게 작용 | 부하를 통제하지 않은 시간 수치는 쓰지 않는다 |
| 측정 앞뒤 카나리가 계속 초과 | 5분간 CPU 를 7배로 쓰면 발열로 클럭이 내려감 | 기준을 완화하지 않고 **판정 문구를 정정** — "절대 시간 못 믿음, 배율만 읽을 것" |
| Anthropic Admin API 로 실청구 조회 | **개인 계정에서 사용 불가** (조직 + admin 역할 필요) | 로컬 집계로 회귀 |
| **쌓인 행 수를 수요의 근거로 읽음** | "저장소마다 스냅샷이 2개"를 재분석 근거로 볼 뻔했다 — 실제로는 저장소마다 실제 하나 + **평가용 픽스처 하나**(`key_source='eval'`)였다 | 값어치를 잴 때는 **행 수가 아니라 그 행이 어떻게 생겼는지**를 본다. 증분 색인이 그래서 보류됐다 |

## 평가셋 · 판정

| 증상 | 원인 | 해결 |
|---|---|---|
| 청크 개선이 지표에 안 잡힘 | 판정이 헐거워 **사실상 파일 검색**을 재고 있었다 (`id_05` 정답비율 100%) | `answers[]` 복수 표현 + 판정 조이기 (100% → 25%) |
| 질의와 정답이 어긋남 | "**요청** 스키마"인데 정답이 전부 Response | 진짜 요청 스키마로 교체 → 5위 → **1위** |
| 정답을 한 파일로 못 박았는데 8파일에 12번 나옴 | 그 파일이 특별할 이유가 없었다 | 질의 의도를 전용 클래스 쪽으로 변경 (134위 → 18위) |
| **정답 없는 질문으로 평가** | 저장소에 없는 기능을 물어 "검색이 나쁜지 코드가 없는지" 구분 불가 | 소스를 먼저 읽고 **정답이 실재하는 질의만** 채택 |
| 한 언어에서만 맞는 규칙을 만듦 | 평가셋이 Java 저장소 하나뿐이었다 | 저장소를 하나 더 붙이자 **그날 결함이 잡혔다**(크기 검사 없는 분기) |
| 사전 확장이 4개를 악화 | 확장은 **이미 잘 맞는 질의에 노이즈**를 더한다 | 전부 되돌림 (부분 수정으로 될 문제가 아니었다) |
| 행 번호 정확도가 파서 오탐 섞임 | 이름 언급(`String`)을 "그 줄에 있다"는 주장으로 해석 | **코드 인용만 판정**(`CODE_LIKE`) |
| **정답 없는 질문을 인용 정확도에 섞으면 지표가 조용히 희석됨** | `_cited()` 는 `answers` 없는 질의에서 **언제나 False** — 분자에서 빠진 채 분모만 늘린다 | 거절 질의는 `ABSENT_SETS` 로 분리하고 `refusal_accuracy` 로 따로 센다. 인용 정확도 모수에서 뺀다 |
| 답변이 옳게 거절했는데 하네스가 전부 0점 | 프롬프트의 거절 문구를 고쳤는데 하네스의 매칭 문자열이 그대로 | 문구를 `claude_client.REFUSAL_PHRASES` 상수로 두고 **프롬프트에 실재하는지 검사**. f-string 보간은 금지 — 검사가 동어반복이 된다 |
| 채택 판정이 갈림 | 기준에 **집계 단위**를 안 정함 (합산이면 채택, 저장소별이면 기각) | **저장소별 모두 동률 이상**으로 확정. 기준은 측정 **전에** 못 박을 것 |

## 프론트

| 증상 | 원인 | 해결 |
|---|---|---|
| 에러가 `[object Object]` | FastAPI 검증 오류의 `detail` 은 **배열** | `errorMessage(detail, status, fallback)` |
| `App → Chat → App` 순환 import | 공용 타입을 App 에서 export | `api.ts` 로 분리 |
| 랜딩에 스크롤바가 생김 | `min-height:100vh` + padding | `box-sizing: border-box` |
| 실패한 질문이 새로고침 후 사라짐 | 백엔드는 **성공 후에만** 저장하는데 화면엔 남김 | 전송 중 질문은 `messages` 가 아니라 `pending` 에 |

## 테스트 · 검증 · 도구

| 증상 | 원인 | 해결 |
|---|---|---|
| **개발 DB 가 오염됨** (카운터가 상한의 2배) | 파일만 `tmp_path` 로 격리하던 fixture 가 **DB 경로를 못 막음** | fixture 에서 DB 를 명시적으로 끔. **저장소를 바꾸면 격리도 바뀐다** |
| 테스트가 갑자기 11초 느려짐 | 엔드포인트에 외부 호출(tarball)을 추가했는데 fixture 가 그것만 안 막아 **진짜 GitHub 을 때렸다** | **엔드포인트에 외부 호출을 더하면 그 테스트 격리도 함께 바꾼다.** 실행 시간이 갑자기 늘면 네트워크를 의심할 것 |
| `sys.modules` 를 치환했는데 대역이 안 먹음 | `from pkg import mod` 는 **패키지 속성을 먼저** 본다. 이미 import 된 서브모듈은 그 속성이 실물을 가리킨다 | 모듈 객체의 **함수를 직접** 갈아끼운다(`monkeypatch.setattr(mod, "fn", ...)`) |
| `shutil.which()` 가 설치된 도구를 못 찾음 | `pip install` 은 venv 의 Scripts/bin 에 넣는데, 인터프리터를 직접 부르면(`.venv/Scripts/python.exe -m ...`) 그 디렉터리가 PATH 에 없다 | **인터프리터 옆을 먼저** 찾는다 (`Path(sys.executable).parent`) |
| `.env` 를 바꾸자 무관한 테스트가 깨짐 | 테스트가 설정 실제 값에 의존 | 각 모듈이 **값을 복사**해 가므로 `monkeypatch` 로 전부 갈아끼운다 |
| 마이그레이션이 NOT NULL 위반 | 옛 기록에 `source`·`cached` 없음 | 화면이 지금까지 해석해 온 **같은 값**으로 채워 고정 |
| 하네스를 만들고 안 돌림 | 품질에서 먼저 갈려 실행할 이유가 없어짐 | **돌려 보니 설계 결함이 나왔다** — 만든 것은 한 번은 돌릴 것 |
| 기록을 읽는 하네스가 `KeyError` | 읽는 쪽이 팔 이름(`"rag"`,`"full"`)을 하드코딩 — 한 팔만 돈 기록에 그 키가 없다 | 기록에 `arms` 를 남기고 읽는 쪽은 `rec.get("arms", ...)` 로 폴백. **기록 모양을 바꾸면 읽는 쪽을 같이 본다** |
| **측정이 0.9초에 `sss` 로 끝나고 로그가 안 생김** | `conftest` 의 opt-in 게이트는 마커마다 **따로** 묻는다. 모듈에 `evaluation`·`billed` 가 둘 다 걸려 있으면 `-m billed` 만으로는 `evaluation` 때문에 건너뛴다 | `-m "billed and evaluation"`. **돌았는지는 실행 시간과 로그 파일로 확인한다** — 통과(`passed`)가 아니라 |
| 거절 정확도가 0.0 인데 답변은 다 옳음 | 규정 문구 `검색된 범위에는 없습니다` 를 모델이 "검색된 **코드** 범위에는 … 없습니다"로 쓴다. 한 단어가 끼어 연속 매칭이 깨진다 | 문구 매칭은 **대리 지표**다. 절대값이 아니라 같은 자를 댄 경로끼리의 **비교**로만 읽는다. 자를 고치면 기준선을 다시 잰다 |
| 변이를 넣었는데 테스트가 그대로 통과 | **변이가 동작을 안 바꿨다.** 순서 정렬 키를 이름 길이로 잡았는데 두 이름의 길이가 같았다 | 변이 테스트는 **변이가 실제로 동작을 바꿨는지부터** 확인한다. 안 그러면 "테스트가 약하다"고 잘못 판정한다 |
| 문서가 조용히 낡음 | `.env.example` 을 손으로 갱신 | 키 일치를 **테스트로 고정**(양방향 + 허위 통과 방지) |
| **`.env` 를 고쳤더니 API 키를 못 읽음** | PowerShell `Out-File -Encoding UTF8` 이 **BOM 을 붙인다** | BOM 없이 쓴다(`UTF8Encoding $false`). 첫 4바이트를 확인할 것 |
| **소스 파일 한글이 통째로 깨지고 줄이 뭉개짐** | 상수 하나 바꾸려고 `Get-Content -Raw \| ... \| Set-Content` 로 왕복시켰다. 읽기는 ANSI(cp949)로 해석하고 쓰기는 UTF-8이라 **글자와 줄 구조가 함께 깨진다** | 한글이 든 파일은 PowerShell 로 치환하지 말 것. 편집 도구로 고친다 |
| 과금 측정이 조용히 skip | 마커가 둘(`evaluation`+`billed`)인데 `-m billed` 만 지정 | `-m "evaluation and billed"`. skip 사유는 `-rs` 로 본다 |
| **문서를 한 줄 고쳤는데 diff 가 파일 전체로 뜬다** | 개행 정규화. `autocrlf=true` 인데 워킹트리 개행이 **파일마다 다르다** — `plan.md`·`CLAUDE.md` 는 CRLF, `TROUBLESHOOTING.md`·`트러블슈팅.md`·`플로우.md` 는 LF | 고치기 전에 **그 파일의** 개행을 확인한다. 도구를 Python 으로 바꿔도 안 풀린다 — `.gitattributes` 로 현상 고정. 상세는 [06-stage5.md](docs/log/06-stage5.md) 의 분할 절 |

---

## 지금도 유효한 운영 주의

- **웹을 열기 전에 `cd Back; docker compose up -d`** — 안 그러면 대화 기능이 통째로 사라진다
- **평가 기록을 비교할 때 소스 캐시(`cache/eval_sources/`)를 지우지 말 것** — 지우면 최신 소스를
  받아 와 측정끼리 입력이 달라진다
- **`pytest -m evaluation` 은 수 분, `-m "evaluation and billed"` 는 과금**된다.
  기본 실행에서 제외돼 있다
- **낡은 색인 1건(air 평가용)은 정상 상태다** — 다음 평가 실행 때 자동 해소된다
- 다중 워커로 띄울 거라면 `DATABASE_URL` 이 **필요하다** (파일 경로는 상한이 사실상 안 걸린다)
- 모델을 바꾸면 **배치 크기·청크 상한·평가 기준선을 다시 재야 한다** — 한 값이 다른 값을 바꾼다
