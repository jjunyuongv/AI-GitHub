# 코드 검색(임베딩) 개선 트러블슈팅

> ⚠️ **이 문서는 2026-08-18 에서 멈춰 있다.** 그 시점까지의 상세 경위로만 읽을 것.
>
> - **증상으로 찾을 때는 [TROUBLESHOOTING.md](TROUBLESHOOTING.md)** — 여기 내용도 한 줄씩 들어가 있고,
>   그 뒤에 겪은 것까지 이어진다
> - **전체 작업 로그는 [docs/log/](docs/log/)** — 그 색인이 [plan.md](plan.md) 다.
>   이 문서가 다루는 시기는 [02-stage3.md](docs/log/02-stage3.md) ·
>   [03-stage4.md](docs/log/03-stage4.md) 에 있다
>
> 이후 바뀐 것들(이 문서에는 없다): 청킹 규칙 버전·재색인 경로 · 평가셋 v2(판정 조이기) ·
> 작은 저장소 RAG 우회 · 소형 모델 기각 · **e5-large int8 양자화 채택**(2.52배) ·
> 사용량/남용방지 DB 이전 · 행 번호 정확도 50% → 75%.
> 특히 §9 말미의 "토큰 상한, 아직 실측하지 못했다"는 **그 뒤 측정해서 800자로 되돌렸다.**

RepoDive 의 `/chat` 은 질문과 관련된 코드 청크를 검색해 프롬프트에 넣는다.
대화가 **소스 코드를 근거로 답하지 못하던 것**을 두 라운드에 걸쳐 고친 과정이다.

| 라운드 | 기간 | 대상 저장소 | 문제 |
|---|---|---|---|
| §0~7 | 2026-08-16 | `jjunyuongv/Air` (Spring Boot, Java 41 + CSS 6 파일) | 검색이 **한국어 질문에서 거의 동작하지 않았다** |
| §8 | 2026-08-17 | `jjunyuongv/marryday` (FastAPI + React/Three.js, 181 파일) | **큰 저장소가 통째로 색인되지 못했다** |
| §9 | 2026-08-17~18 | 위 두 저장소 (평가셋 33질의) | **청킹이 정답을 버리고·자르고·노이즈를 올렸다** |

---

## 0. 증상

대화가 코드를 못 보고 "추정"만 했다.

> **사용자**: 로그인 관련 부분은 어디에 있어?
> **답변**: `SecurityConfig.java` … 로그인/인가 처리 로직이 있을 것으로 **추정됩니다 (추정)**
> **사용자**: 안에 코드는 못보는거야?
> **답변**: 네, 맞습니다. 제공된 정보는 README, build.gradle, **파일 경로 목록뿐**입니다.

---

## 1. 첫 번째 원인 — 소스 코드가 시스템에 아예 없었다

`context_builder.py` 가 LLM 에 넣던 것은 이게 전부였다.

| 항목 | 내용 |
|---|---|
| 레포 메타 | 이름·설명·언어·스타 |
| README | 3,000자까지 |
| 매니페스트 | `build.gradle` 등 루트 1depth |
| 파일 목록 | **경로 문자열만** (내용 없음) |

검색을 개선할 문제가 아니라 **검색할 대상이 없던 문제**였다.

### 해결: tarball 1회로 소스 수집

파일마다 `/contents` API 를 부르면 요청이 파일 수만큼 늘어 rate limit 이 먼저 터진다.
`GET /repos/{o}/{r}/tarball` 로 저장소를 통째로 받으면 **요청 1회**로 끝난다.

```
47개 파일 / 111,857자 / 1.4초 / GitHub 요청 2회
(파일별 방식이었다면 48회)
```

---

## 2. 청킹 — 클래스를 통째로 두면 검색이 망가진다

처음엔 "2,400자 이하면 클래스를 통째로 한 청크"로 했다. 결과가 이상했다.

```
질문: "비밀번호는 어떻게 암호화해?"
  0.6220  SafeEduService.java   ← 클래스 통째
  0.6573  OpsMnalService.java   ← 클래스 통째
  0.6796  CbnSrvcService.java   ← 클래스 통째
```

**클래스 하나를 한 벡터로 만들면 그 벡터는 "이 클래스가 하는 모든 일"의 평균이 된다.**
평균 벡터는 어떤 질문과도 어중간하게 가까워서, 질문과 무관한 컨트롤러들이 늘 상위를 차지했다.

### 해결

| 조치 | 효과 |
|---|---|
| 컨테이너(클래스·인터페이스)는 **크기와 무관하게 메서드 단위로 분해** | 청크 71 → 145개, 메서드별로 검색됨 |
| `import` 노드 제외 | 122청크 중 **47개가 import 덩어리**였다. 중앙값 402자 → 1,068자 |
| 주석만 있는 클래스 헤더 제외 | `/*작성자*/` + 선언만 있는 청크가 파일마다 비슷해 노이즈였다 |
| 40자 미만 부스러기 제거 | 닫는 괄호·주석 한 줄 |

파싱이 깨진 파일은 라인 기준으로 자른다 — 청킹 실패가 그 파일을 검색에서 통째로 지우면 안 된다.

---

## 3. 두 번째 원인 — 임베딩 모델이 한국어를 몰랐다

소스를 넣고 청킹을 고쳐도 한국어 질문이 계속 실패했다. 같은 정답
(`SecurityConfig.java:41` 의 `return new BCryptPasswordEncoder();`)을 두고
**질문 표현만 바꿔** 순위를 재봤다.

| 질의 | 정답 순위 (145개 중) |
|---|---|
| "비밀번호는 어떻게 암호화해?" | 17위 |
| "비밀번호 암호화에 뭘 써?" | 33위 |
| **"PasswordEncoder 어디서 만들어?"** | **1위** |
| **"BCrypt 쓰는 곳"** | **1위** |

영문 식별자로 물으면 1위, 한국어 개념어로 물으면 17~33위. 원인이 명확해졌다.

`jinaai/jina-embeddings-v2-base-code` 의 모델 설명을 다시 읽으니:

> Multilingual (**English, 30 programming languages**)

여기서 '다국어'는 **프로그래밍 언어 30종**이고 **자연어는 영어 전용**이었다.
한국어 지원이 애초에 없었다.

앞서 "로그인" 질문이 맞았던 것도 그 코드에 한국어 주석
(`logger.info("로그인 성공! ...")`)이 있어서 한국어끼리 맞은 것이지,
한국어→영문 개념 매핑이 된 게 아니었다.

### 해결: 다국어 모델로 교체 — 이번 작업의 가장 큰 성과

`intfloat/multilingual-e5-large` (1024차원, ~100개 언어)

| 지표 | jina-code (768) | **e5-large (1024)** |
|---|---|---|
| **한국어 Recall@8** | 0.17 (2/12) | **0.67 (8/12)** |
| 한국어 평균순위 | 67.9위 | **12.8위** |
| 한국어 MRR | 0.0665 | **0.4909** |
| 식별자 Recall@8 | 0.80 | 0.80 (유지) |

한국어 12개 질의가 **전부** 좋아졌고, 나빠진 것은 하나(1위→2위)뿐이었다.

**대가**: 임베딩 1.9배 느려짐(38초→73초), 모델 3.3배 커짐(655MB→2.1GB),
입력 한도 8192→512 토큰이라 **청크 15개(10.3%)가 잘림**.

---

## 4. 시도했다가 되돌린 것 — 도메인 용어 사전

### 아이디어

사용자는 "결재 승인은 어디서 처리해?" 라고 묻는데 코드에는 한국어가 한 글자도 없고
`ApprovalController.approve` 만 있다. 이 어휘 격차를 사전으로 잇는다.

정적 파일로는 둘 수 없다 — 임의의 저장소를 받는 도구라 프로젝트마다 쓰는 말이 다르다.
그래서 **인덱싱 시점에 자동 생성**하도록 만들었다.

1. tree-sitter 로 식별자 추출 → camelCase 분해 → 프레임워크 노이즈 필터
2. **Claude API 1회 호출**로 식별자 → 자연어 용어 매핑
3. 저장소 단위로 DB 저장
4. 질의 시 사전 조회만 하고 매칭된 식별자를 질의에 덧붙임 (API 호출 없음)

### 사전 자체는 잘 만들어졌다

식별자 120개 → 용어 88개. 이 프로젝트의 항공 도메인 축약어가 정확히 풀렸다.

| 코드 | 사전이 만든 용어 |
|---|---|
| `Cbn` | 객실, 캐빈 |
| `Mnal` | 매뉴얼, 지침서 |
| `Atch` | 첨부파일 |
| `Ctnt` | 내용, 본문 |
| `udt` | 수정일시 |

### 그런데 검색 성적은 반반이었다

| 질의 | 사전 OFF | 사전 ON |
|---|---|---|
| ko_04 결재 승인은 어디서 처리해? | 18 | **2** |
| ko_05 결재를 반려하는 기능이 있어? | 53 | **5** |
| ko_01 비밀번호는 어떻게 암호화해? | 4 | **6** |
| ko_03 관리자만 어떻게 막았어? | 47 | **56** |
| ko_06 일정을 저장하는 코드는? | 1 | **2** |
| ko_07 일정 삭제는 어떻게 동작해? | 1 | **3** |

**못 찾던 둘은 크게 좋아졌지만, 이미 잘 찾던 넷이 나빠졌다.**

원인은 확장이 **이미 잘 맞는 질의에 노이즈를 더하는 것**이었다.
"일정"이 `Schedule` `Schedules` `event` `Recurring` 을 한꺼번에 붙여 초점을 흐리고,
"관리자"의 `Admin` 은 SecurityConfig 보다 `/admin/**` 매핑이 많은 UserController 를 끌어올렸다.

### 판단: 비용은 들었는데 성과가 충분하지 않아 제거

- **비용**: Claude API 호출 **$0.0375** (입력 1,993 / 출력 3,348 토큰), 저장소마다 발생.
  여기에 신규 모듈 3개·테이블 1개·테스트 12개의 유지 부담이 더해진다.
- **성과**: Recall@8 은 0.67 → 0.83 으로 올랐지만 MRR 은 0.4909 → 0.4386 으로 **떨어졌다**.
  1위였던 질의들이 밀려난 탓이다. 얻은 것과 잃은 것이 상쇄되는 수준이었다.

들인 비용과 복잡도에 비해 **분명한 이득이라 말할 수 없어 전부 되돌렸다.**
코드·스키마·DB 테이블·테스트·측정 기록을 모두 제거하고 사전 도입 이전 상태로 복귀했다
(평가셋 재실행으로 17개 질의가 전부 이전 순위와 일치함을 확인).

`schema.sql` 에는 왜 뺐는지 주석만 남겼다 — 다시 붙일 일이 있을 때를 위해.

---

## 5. 측정을 어떻게 했나

감으로 판단하지 않기 위해 평가셋을 먼저 만들었다.

- **질의 17개** — 한국어 개념어 12 + 영문 식별자 5.
  특정 문장에 과적합하지 않도록 보안·전자결재·일정·채팅·회원·신고·유틸에 걸쳐 흩었다.
- **정답은 (파일 + 포함 문자열)로 표기** — 청킹 규칙을 바꾸면 청크 경계가 달라져
  청크 ID 기반 정답은 전부 무효가 되기 때문.
- **질의를 지어내지 않았다** — 저장소 소스를 먼저 읽고 정답이 실재하는 것만 채택했다.
  (초기에 "예약 기능"을 물었다가 저장소에 0줄이어서 판단 불가에 빠진 적이 있다.)
- 지표: 정답 순위, MRR, Recall@8/3/1
- 실행: `pytest -m evaluation` (네트워크와 수십 초가 필요해 일반 테스트에서 제외)
- 비교: 관리자 페이지 `/admin/search` 에서 모델별로 나란히

---

## 6. 측정하다 틀릴 뻔한 것들

| 함정 | 내용 |
|---|---|
| **"잘린 청크 0개"** | 토크나이저가 이미 한도(512)에서 잘라 반환한다. 초과(`>`)로 세면 영원히 0 — **도달(`>=`)**로 세야 실제 15개가 잡힌다 |
| **피크 메모리 68MB** | `tracemalloc` 은 파이썬 힙만 잰다. ONNX 네이티브 할당은 안 보여서 2.1GB 모델이 68MB 로 찍혔다 |
| **테스트가 `.env` 에 의존** | 모델을 바꾸자마자 테스트가 깨졌다. 테스트는 환경 설정과 무관해야 한다 |
| **정답 없는 질문으로 평가** | 저장소에 없는 기능을 물어 "검색이 나쁜지 코드가 없는지" 구분 불가 |

---

## 7. 그 밖의 함정

**pgvector**
- `operator does not exist: vector <=> double precision[]` → 쿼리에 **`%s::vector` 캐스트 필수**.
  `register_vector` 를 등록해도 파이썬 `list` 는 `float8[]` 로 전송된다
- 차원이 다른 모델을 A/B 하려면 **테이블을 분리**해야 한다. `vector(768)` 은 차원이 고정이라
  모델 이름 컬럼을 추가하는 방식으로는 1024차원을 담을 수 없다

**fastembed**
- `batch_size` 기본값(256)이 오히려 느리다 → **8로 낮추니 1.7배 빨라졌다**
  (짧은 청크까지 배치 내 가장 긴 것에 맞춰 패딩되기 때문).
  단 이 값은 **모델마다 다르다** — e5-large 로 바꾼 뒤에는 32 가 최적이었다(§8)
- 모델 캐시 기본 경로가 OS 임시 폴더다 → 윈도우가 청소하면 수백 MB 를 다시 받는다
- **`query_embed()` 는 접두어를 붙여주지 않는다** (그냥 `embed()` 를 호출).
  E5 계열은 `"query: "` / `"passage: "` 가 필수라 직접 붙여야 한다
- `.env` 에 접두어를 쓸 때 **따옴표 필수** — 없으면 파서가 끝 공백을 잘라 `query:질문` 이 된다
- `bge-m3` 는 fastembed 0.8.0(최신)에 없다. E5 계열은 `multilingual-e5-large` 하나뿐

**인프라**
- 채팅 UI 가 안 보이면 DB(Docker) 미기동을 의심할 것. `session_id` 가 null 이면
  프론트가 대화 영역을 통째로 숨긴다. 빠른 진단: `GET /chat/{아무 uuid}` → 404면 정상, 503이면 끊김
- pgvector 확장은 공식 `postgres:17` 이미지에 없다 → `pgvector/pgvector:pg17` 사용

---

## 8. 두 번째 라운드 — 다른 저장소에서 또 코드를 못 봤다

기간: 2026-08-17 · 대상 저장소: `github.com/jjunyuongv/marryday`
(FastAPI + React/Three.js, 소스 181파일 / 226만 자)

### 증상 — 1라운드와 **같은 대화가 반복됐다**

> **사용자**: body_analysis.py 안에 코드 보여줘
> **답변**: 실제 소스 코드 내용이 포함되어 있지 않습니다. **파일 목록에만 존재가 확인**될 뿐…
> **사용자**: 실제 코드는 못보는거야?

여기에 더해, 질문할 때마다 "이 저장소의 코드를 처음 읽고 있습니다" 안내가 다시 떴다.

### 원인이 세 겹이었다

| # | 원인 | 근거 |
|---|---|---|
| 1 | tarball 이 `MAX_ARCHIVE_BYTES`(80MB)를 넘어 수집이 **413 으로 실패** | 재현: `RepoAccessError 저장소 아카이브가 너무 큽니다`, 9.6초 |
| 2 | 실패해도 **질문마다 다시 받았다** | `runs.jsonl` 의 chat 6건 전부 `fetch_ms` 9~11초 (정상 저장소는 6~8ms) |
| 3 | 프론트 안내가 색인 상태와 **무관한 시간 추정**(8초 초과 시 표시) | 이미 색인된 저장소에서도 떴다 |

3D 에셋(`public/3D/`)이 든 저장소라 아카이브가 컸다. **tarball 크기는 인덱싱 부담과 비례하지 않는다** —
우리가 뽑아 쓰는 건 소스뿐인데 이미지·모델 파일까지 상한에 포함됐다.

원인 2는 **회귀**였다. `schema.sql` 과 `indexer.py` 주석에 "청크 0개도 완료로 남긴다,
안 그러면 질문할 때마다 tarball 을 다시 받는다"고 적혀 있었는데, 차원별 테이블을 도입하며
완료 판정을 `indexed_at` → **청크 행 존재 여부**로 바꾸면서 그대로 깨졌다.
(테이블이 둘이 되어 시각 하나로는 어느 쪽이 끝났는지 못 가린 게 이유였다.)
**주석으로 의도를 적어 둔 것만으로는 회귀를 막지 못한다 — 테스트로 고정해야 한다.**

### 상한을 풀자 진짜 문제가 드러났다

| 항목 | marryday | (1라운드) Air |
|---|---|---|
| 소스 파일 | 181개 | 47개 |
| 청크 | **1,683개** | 145개 |
| 수집 / 청킹 | 15~20초 / 1.3초 | 1.4초 / 0.09초 |
| 임베딩 | **약 29분** | 73초 |

첫 질문 때 동기로 인덱싱하는 구조로는 감당할 수 없는 규모였다.

### 배치 크기는 **모델을 바꾸면 다시 재야 한다**

§7 에 "batch_size 를 8 로 낮추니 1.7배 빨라졌다"고 적어 뒀는데, 그건 jina-code(768) 시절 값이었다.
e5-large(1024, 입력 512토큰)로 바꾼 뒤 실측(청크 200개, 평균 982자):

| batch_size | 청크당 | 1,683청크 환산 |
|---|---|---|
| **8** (당시 설정) | 2,461ms | 69분 |
| 16 | 1,616ms | 45분 |
| **32** | **1,032ms** | **29분** |
| 64 | 1,173ms | 33분 |

**8 → 32 로 2.4배 빨라졌다.** `threads` 를 코어 수로 명시하는 것은 오히려 느렸다
(onnxruntime 기본값 0 = 자동이 낫다).

나머지 병목 후보는 전부 무혐의였다 — 임베딩은 이미 배치 호출, DB 는 `executemany`
(145청크 저장 0.25초), 모델은 싱글턴 + 기동 시 예열.

### 해결: 백그라운드 인덱싱 + 상태를 DB 에 남기기

| 조치 | 내용 |
|---|---|
| 아카이브 상한 | 80MB → **500MB**, 받는 버퍼를 `SpooledTemporaryFile`(32MB 초과분은 디스크)로. 상한만 올리면 수백 MB 가 전부 메모리에 올라온다 |
| `snapshot_index_status` 테이블 | pending / running / completed / failed + 청크 진행 + 실패 사유. 키는 `(snapshot_id, table_name)` — 차원별로 청크 테이블이 갈려 모델마다 상태가 따로 간다 |
| 완료 판정 이전 | 청크 행 존재 → **이 표의 completed**. 청크 0개도 완료는 완료 (원인 2의 근본 수정) |
| 백필 | 이미 색인된 스냅샷을 completed 로 채운다. 안 하면 기동하자마자 통째로 재색인한다 |
| 인덱싱 시점 | 첫 질문(동기) → **`/analyze` 직후 백그라운드**. 사용자가 요약을 읽는 동안 진행된다 |
| 진행률 | 파일 수가 아니라 **청크 수** 기준. 임베딩 배치마다 보고 |
| 안내 문구 | 시간 추정(8초) 제거 → `GET /chat/{id}/index` 를 3초마다 물어 실제 상태·남은 시간·실패 사유를 표시 |

**미완료 상태에서는 검색을 아예 하지 않는다.** 부분 인덱스로 검색하면 아직 임베딩하지 않은 코드가
'저장소에 없는 코드'처럼 보여 틀린 답을 만든다. 그동안은 요약만으로 답하고, 화면이 그 사실을 알린다.

### 이번에도 측정하다 틀릴 뻔한 것

| 함정 | 내용 |
|---|---|
| **ETA 가 두 배로 나왔다** | 남은 시간을 `경과 / 끝난 청크` 로 계산하는데 기준 시각이 **수집 시작**이었다. tarball 15~20초가 청크당 속도에 섞여 29분짜리가 **58분**으로 표시됐다 → 청크 수가 확정되는 시점(임베딩 직전)에 기준 시각을 다시 찍는다 |
| 죽은 `running` | 인덱싱 스레드는 프로세스와 함께 사라진다. 서버를 내리면 그 행이 running 으로 남아 재시도를 **영원히** 막는다 → 기동할 때 pending 으로 되돌린다 |
| 시작 판정과 표시의 틈 | "아직 안 했네"를 조회한 뒤 표시하면 동시 요청 둘이 함께 인덱싱을 시작한다 → `INSERT … ON CONFLICT … WHERE` **한 문장**으로 판정과 표시를 같이 한다 |

### 결과

1,683청크를 22분에 색인 완료. 화면에서 실패했던 그 질문이 이제 맞는다.

| 질의 | 1위 결과 |
|---|---|
| body_analysis.py 안에 코드 보여줘 | **`routers/body_analysis.py` 569-633행** |
| 체형 분석은 어떻게 해? | `pages/Analysis/BodyAnalysis.jsx` |
| 이미지 합성은 어디서 처리해? | `routers/composition.py` |

---

## 9. 세 번째 라운드 — 청킹 자체를 고치다

기간: 2026-08-17 ~ 18 · §0~7 에서 "다음에 고친다"고 적어 두고 미뤘던 결함 셋을
하나씩 고치고 **매 단계 평가**했다(주 지표 Recall@8).

### 원칙: 세 결함을 한 번에 고치지 않았다

한꺼번에 고치면 어느 수정이 어느 변화를 만들었는지 알 수 없다. 그래서 2a → 2b → 2c 로
나누고 단계마다 평가셋 17질의를 다시 돌렸다. **그 덕에 2a·2b 가 한국어 검색을 깎아먹는 것을
중간에 알아챘고**, 2c 가 그것을 되돌리는지로 판단할 수 있었다.

### 2a. 짧은 클래스 헤더를 버리고 있었다

`public class ChatHandler extends TextWebSocketHandler` 가 인덱스에 **아예 없었다.**
헤더가 `MIN_CHUNK_CHARS`(200자)에 못 미치거나 "주석 + 선언"뿐이면 노이즈로 보고 버렸기 때문이다.

평가 질의 id_04("TextWebSocketHandler 상속")는 순위가 낮았던 게 아니라 **정답 자체가
없었다.** 클래스가 무엇을 상속하는지는 그 파일에서 그 한 줄에만 있다.

버리는 조건을 없애자 **부작용이 나왔다** — 살아난 헤더가 병합 사슬을 하나 늘려
메서드 둘이 한 청크로 뭉쳤다. `_merge_small` 이 작은 조각을 **연쇄로** 붙이고 있었던 것이다.
"작은 조각은 이웃 **하나**에만 붙는다"로 바꿨다(클래스를 통째로 두지 않는 것과 같은 이유).

> **교훈**: 주석으로 의도를 적어 둔 것만으로는 회귀를 막지 못한다.
> `schema.sql` 과 `indexer.py` 에 "청크 0개도 완료로 남긴다"고 써 뒀는데도 §8 의 회귀가 났고,
> 이번 헤더 규칙도 테스트가 없어 병합 부작용을 측정으로만 발견했다. 둘 다 테스트로 고정했다.

### 2b. 청크가 조용히 잘리고 있었다 — 상한을 실측으로 역산

e5-large 의 한도는 512 토큰이고 **넘는 부분은 토크나이저가 말없이 버린다.**
`MAX_CHUNK_CHARS` 가 2,400 이던 시절 182청크 중 14개(7.7%)가 그 상태였다.

문자/토큰 비율을 재서 상한을 거꾸로 계산했다 — 최악 1.60 · 5분위 1.89 · 중앙값 2.96
(한국어 주석이 섞인 청크가 토큰을 가장 많이 먹는다. 한글은 토큰당 1~1.5자).
`512 × 1.60 ≈ 819` → 접두어 몫을 빼고 **800**. 잘림은 0 이 됐다.

**잘린 14개 중 10개가 CSS 였다.** 한국어 주석 + 긴 선언이 뭉쳐 있어서다.

그런데 **한국어 Recall@8 이 0.67 → 0.50 으로 떨어졌다.** 잘게 쪼갠 조각이 상위를 차지한 탓이다.

### 2c. 저정보 청크에 감점 — CSS 가 노이즈의 주범이었다

악화된 질의의 상위 결과를 직접 들여다봤다.

```
질문: "채팅 연결이 끊기면 무슨 일이 일어나?"   (정답: ChatHandler.afterConnectionClosed)
  1. arch.css       0.2120
  2. view.css       0.2164
  3. style.css      0.2168
  4. ChatSocketConfig.java (헤더)
  5. style.css      0.2172
```

CSS 에 한국어 주석(`/* 페이지네이션 링크 스타일 */`)이 많아 **한국어 질의와 곧잘 맞는다.**
게다가 2b 에서 잘게 쪼개지며 **온전히 임베딩되어 오히려 더 잘 맞게 된** 역설이 있었다
(2,400자 시절에는 512 에서 잘려 뒷부분이 벡터에 없었다).

| 결정 | 이유 |
|---|---|
| 제외가 아니라 **감점** | "이 버튼 스타일 어디서 정의해?" 는 CSS 가 정답이다 |
| 저장이 아니라 **검색 단계**에서 | 벡터가 그대로라 **감점값 A/B 가 수초**다. 저장 단계면 규칙마다 재인덱싱 6분 |
| 후보를 `limit × 5` 로 넓게 | 상위 8개만 가져와 감점하면, 감점 대상에 밀려 못 들어온 근거가 영영 안 보인다 |
| **상속 선언은 감점 제외** | id_04 의 정답이 바로 그 헤더다. 뭉뚱그리면 2a 가 살린 것을 2c 가 도로 망가뜨린다 |
| 감점값 0.03 | 0.03 부터 회복되고 **0.05 이상은 변화가 없다**(사실상 '제외'가 되는 구간) → 포화 직전에서 멈춤 |

저정보 판정 62/236 = CSS 35 + Java 헤더 27(작성자 주석 + 필드 선언만).

### 단계별 결과 — 이 표가 판단의 근거였다

| 지표 | baseline | 2a | 2b | **2c** |
|---|---|---|---|---|
| 잘린 청크 | 15 | 14 | **0** | **0** |
| 청크 수 | 145 | 182 | 236 | 236 |
| id_04 순위 | **없음** | 1 | 1 | **1** |
| 식별자 Recall@8 | 0.80 | 1.00 | 1.00 | **1.00** |
| 식별자 MRR | 0.6000 | 0.8667 | 0.8667 | **0.9000** |
| 한국어 Recall@8 | 0.67 | 0.58 | **0.50** | **0.67** |
| 한국어 MRR | 0.4909 | 0.4573 | 0.4122 | **0.5028** |
| 전체 Recall@8 | 0.71 | 0.71 | 0.65 | **0.76** |
| 전체 MRR | 0.5230 | 0.5777 | 0.5458 | **0.6196** |

2a·2b 만 보면 실패한 작업처럼 보인다. **중간에 멈추지 않고 원인(노이즈)을 찾아 2c 로 간 것이
맞았다** — 최종적으로 baseline 대비 모든 지표가 같거나 좋아졌다.

### 평가셋을 다른 언어로 넓히자 곧바로 결함이 나왔다

Java 저장소 하나(17질의)로만 판단하고 있었다. Python/React 저장소(`marryday`)에 질의 16개를
추가하고 하네스를 저장소별로 나눴다. **확장한 바로 그날 결함이 하나 잡혔다.**

`chunk_file()` 이 "정의가 아닌 최상위 요소"를 **크기 검사 없이 통째로** 넣고 있었다.
Java 에는 그런 요소가 거의 없어 안 보이다가 Python/JS 에서 터졌다:

| 통째로 들어간 것 | 크기 |
|---|---|
| `document.addEventListener('DOMContentLoaded', ...)` 한 덩어리 | 4,115자 |
| 긴 프롬프트 상수 `GEMINI_DEFAULT_COMPOSITION_PROMPT = """..."""` | 3,213자 |
| `if __name__ == "__main__":` 블록 | 1,712자 |

청크 11개가 한도에서 잘리고 있었다. 그 분기도 상한을 넘으면 라인 분할로 내려보내 고쳤다.

> **교훈**: 평가셋의 언어가 하나면 그 언어에서만 맞는 규칙을 만든다.
> 저장소 하나를 더 붙이는 것만으로 숨은 결함이 드러났다.

**marryday 측정 (Python/JS, 16질의)**

| 지표 | air (Java) | marryday |
|---|---|---|
| 한국어 Recall@8 | 0.67 | **0.80** |
| 전체 Recall@8 | 0.76 | **0.81** |
| 정답 부재 · 잘린 청크 | 0 · 0 | 0 · 0 |

청킹 개선은 Java 전용이 아니었다. `py_id_06`("BaseModel 을 상속한 요청 스키마") 5위 →
2a 가 Python 에서도 듣는다. 다만 이상치 둘이 평균순위를 끌어올렸다:
- `py_ko_05` **278위** — 정답 파일이 38,705자로 가장 크고 `SegFormer` 가 13파일에 154번
  나와 정답이 같은 주제의 청크 더미에 묻혔다
- `py_id_03` **134위** — `genai.Client` 가 8파일에 12번 나온다. **평가셋 설계 문제로 의심**
  (정답을 한 파일로 못 박았는데 다른 파일도 사실상 정답이다)

### 임베딩이 96분 — 배치 크기를 의심했지만 아니었다

marryday 인덱싱이 4,364청크 / **96분**이 됐다(2b 전 같은 저장소가 22분).
"청크를 하나씩 임베딩하는 게 아닌가" 하고 호출부를 확인했다.

```python
prefixed = [document_text(t) for t in texts]                       # 리스트 전체
for vector in _get_model().embed(prefixed, batch_size=EMBED_BATCH_SIZE):   # 한 번 호출
```

**이미 배치였다**(batch_size=32). 그리고 재측정 결과가 예상과 반대로 나왔다
(marryday 청크 100개, 평균 450자):

| 조건 | ms/청크 |
|---|---|
| `batch=32` | 1,288 |
| `batch=48` | 1,441 |
| `batch=64` | 1,488 |
| **`batch=1`(개별 호출)** | **1,123** |

배치는 배치 내 **최장 청크에 맞춰 패딩**되므로 길이 편차가 크면 오히려 손해다.
다만 같은 `batch=32` 가 몇 시간 전 1,032ms → 지금 1,288ms 로 25% 흔들렸다 —
**96분 연속 100% CPU 뒤라 열 스로틀링이 배치 효과보다 크게 작용**한다. 이 환경에서
배치 튜닝으로 얻을 이득은 측정 노이즈보다 작다고 보고 손대지 않았다.

**22분 → 96분의 진짜 이유**: 청크 수가 아니라 **잘림이 없어진 것**이다.
전에는 1,683청크가 512 에서 잘려 뒷부분을 계산하지 않았다. 즉 잘림이 "빠른 이유"였고,
잘림 0 을 유지하는 한 그 시간은 돌아오지 않는다.

### 그래서 상한을 문자에서 **토큰**으로 옮겼다 (측정 대기 중)

토큰 분포를 재 보니 여유가 컸다.

| | air (236청크) | marryday (4,364청크) |
|---|---|---|
| 토큰 p50 | 143 | 166 |
| 토큰 p95 | 323 | 279 |
| 토큰 max | 375 (여유 137) | 507 (여유 **5**) |
| 문자 p50 / p95 | 410 / 795 | **744 / 798** |
| 문자/토큰 비율 p1 → max | 1.75 → 4.33 | **1.68 → 17.40** |

중앙값이 한도의 30% 정도밖에 안 된다. **그런데 문자 상한을 올려 그 여유를 쓸 수는 없다** —
비율이 10배 벌어져서, 1,200자로만 올려도 11~17% 가 512 를 넘길 위험이 생긴다.
marryday 의 문자 p50 이 744, p95 가 798 인 것도 **거의 모든 청크가 800 상한에 붙어 있다**는
뜻이다(상한이 실제로 자르고 있다).

- `MAX_CHUNK_CHARS` **800 → 2,400** (2b 이전 값으로 복귀). 영어 코드 청크는 다시 크게 유지된다
- 한도는 `_split_by_tokens()` 가 **토크나이저로** 지킨다 — 청킹 후 실제 토큰을 재서
  **넘는 청크만** 그 청크의 실측 비율로 재분할(안전계수 0.9, 최대 3회 반복)
- 토크나이저는 **주입**으로 받는다(`chunk_files(files, count_tokens=..., token_limit=...)`).
  청킹이 임베딩 모델을 직접 부르면 청킹 테스트가 2GB 가중치를 올려야 한다
- 토크나이저에 닿지 못하면 문자 상한만 믿고 진행한다 (인덱싱이 멈추면 안 된다)

**아직 실측하지 못했다** — 검증하느라 143MB tarball 을 짧은 시간에 여러 번 받아
GitHub **아카이브 전용 secondary rate limit**에 걸렸다(core 는 5,000/5,000 남아 있는데 429).
회복 뒤 (1) 잘림 0 유지·청크 수 감소 → (2) air 평가로 Recall@8 회복 여부 → (3) marryday 확인
순으로 볼 것. 다시 받지 않도록 소스를 로컬에 캐시하는 스크립트를 준비해 뒀다.

> **함정**: GitHub 의 tarball 다운로드는 core rate limit(5,000/h)과 **별개의 제한**을 받는다.
> `/rate_limit` 이 여유를 보여줘도 아카이브는 429 가 날 수 있다. 같은 저장소를 반복해서
> 받는 실험은 **로컬 캐시를 먼저** 만들 것.

---

## 10. 현재 상태와 남은 문제

**적용된 것**: tarball 소스 수집(최대 500MB) · tree-sitter 메서드 단위 청킹(짧은 클래스 헤더 유지,
겹침 분할, **토큰 기준 재분할**) · `multilingual-e5-large`(1024차원) + query/passage 접두어 ·
pgvector HNSW 검색 + **저정보 청크 감점** · `/analyze` 직후 백그라운드 인덱싱 + 진행 상태 표시

**한국어 Recall@8: 0.17 → 0.67** (전체 0.76 · marryday 0.81) · **잘린 청크 0** ·
**평가셋 2저장소 33질의**(Java 17 + Python/JS 16) · pytest 192 passed

**남은 문제**

| 문제 | 내용 |
|---|---|
| **토큰 재분할 미측정** | 문자 상한을 2,400 으로 되돌리고 토큰 기준 재분할을 넣었지만 GitHub 아카이브 제한(429)으로 실측을 못 했다. 잘림 0 유지 · 청크 수 · Recall@8 을 확인해야 한다 |
| 인덱싱 시간 | marryday 4,364청크에 96분. 잘림을 없앤 대가다(전에는 512 에서 잘려 22분). 실질적 단축은 소형 다국어 모델이나 청크 수 감소뿐이다 |
| 큰 파일에서 정답이 묻힌다 | `py_ko_05` 278위 — 38,705자 파일에 같은 주제(`SegFormer`) 청크가 154개 |
| 평가셋 채점 기준이 좁다 | `py_id_03` 134위는 검색 문제가 아니라 정답을 한 파일로 못 박은 탓으로 의심된다 |
| 어휘 격차 | "결재"(21위), "관리자 권한"(30위)처럼 코드에 그 한국어가 없는 개념은 여전히 약하다 |
| 평가셋 규모 | 저장소 2개 · 질의 33개. 여전히 표본이 작다 |
| 옛 스냅샷 청크 누적 | 저장소가 갱신되면 새 스냅샷을 다시 색인하고, 옛 청크는 계속 쌓인다 |
